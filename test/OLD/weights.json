[
  {
    "section": "Backslash escapes",
    "example": 12,
    "weight": 0.9,
    "explanation": "Backslash escapes are commonly used to show literal punctuation that would otherwise be parsed (e.g., *, _, [, ], `); if missing or wrong, ordinary text and inline elements can be mis-rendered (unintended emphasis, links), so support is highly important."
  },
  {
    "section": "Backslash escapes",
    "example": 13,
    "weight": 0.9,
    "explanation": "Backslash escapes are a common and fundamental way to include literal punctuation (e.g., to prevent emphasis, links, or other markup); getting them wrong would frequently break or alter ordinary documents and confuse readers, though the specific non-ASCII edge cases shown are slightly less common than core features like headings or links."
  },
  {
    "section": "Backslash escapes",
    "example": 14,
    "weight": 0.9,
    "explanation": "Backslash escapes are commonly used to show literal punctuation that would otherwise be parsed as emphasis, headings, lists, code spans, or links; if not supported many documents (tutorials, READMEs, inline examples) will render incorrectly or misleadingly. This affects fundamental inline syntax and is therefore highly important though slightly less central than core block constructs like headings or code blocks."
  },
  {
    "section": "Backslash escapes",
    "example": 15,
    "weight": 1.0,
    "explanation": "Backslash escapes are widely used to produce literal punctuation and to prevent accidental emphasis (e.g., showing asterisks literally); if a renderer ignores or mishandles them, common text will be incorrectly formatted or misleading, so correct support is critical."
  },
  {
    "section": "Backslash escapes",
    "example": 16,
    "weight": 0.5,
    "explanation": "A backslash at end-of-line for a hard line break is a known but not ubiquitous Markdown idiom; incorrect handling would alter formatting and could confuse readers, but it’s not as fundamental as headings, links, or code blocks."
  },
  {
    "section": "Backslash escapes",
    "example": 17,
    "weight": 0.9,
    "explanation": "Backslash escapes are commonly used to include literal punctuation and to prevent accidental emphasis or link parsing; incorrect handling would frequently produce malformed inline text and confuse readers. This is a core inline feature relied on in many real-world docs, so lack of correct support is highly impactful."
  },
  {
    "section": "Backslash escapes",
    "example": 18,
    "weight": 1.0,
    "explanation": "Code blocks are common and often contain backslashes (regex, code examples, LaTeX); treating backslash escapes inside code blocks would corrupt or hide significant characters and severely misrepresent the content, so correct handling is critical."
  },
  {
    "section": "Backslash escapes",
    "example": 19,
    "weight": 0.9,
    "explanation": "Backslash escapes are a foundational Markdown feature used frequently to show literal punctuation and prevent unintended formatting; getting them wrong (especially the rule that escapes do not apply inside code spans/blocks) would often alter content and confuse readers."
  },
  {
    "section": "Backslash escapes",
    "example": 20,
    "weight": 0.5,
    "explanation": "Autolinks and correct escaping/percent-encoding in URLs are common enough that mishandling can break links or produce misleading display text, but backslashes inside real URLs are relatively rare, so this is moderately important rather than critical."
  },
  {
    "section": "Backslash escapes",
    "example": 21,
    "weight": 0.3,
    "explanation": "This is a fairly niche detail about backslashes inside raw HTML (they should be preserved, not treated as Markdown escapes). It can occasionally alter literal attribute values or regex-like strings if handled wrong, but such patterns are uncommon in typical Markdown documents."
  },
  {
    "section": "Backslash escapes",
    "example": 22,
    "weight": 0.8,
    "explanation": "Backslash escapes are commonly used to include punctuation literally (including inside link destinations and titles) and to avoid accidental emphasis; if a renderer mishandles them it can produce broken or misparsed links or show stray backslashes, which is frequently confusing in real documents."
  },
  {
    "section": "Backslash escapes",
    "example": 23,
    "weight": 0.7,
    "explanation": "This tests backslash escapes inside link destinations and titles—links are a foundational feature and escaping punctuation in URLs/titles appears reasonably often. If unsupported or mis-rendered, links can display incorrectly or break, which is confusing and can mislead readers."
  },
  {
    "section": "Backslash escapes",
    "example": 24,
    "weight": 0.7,
    "explanation": "Fenced code block info strings are common and drive syntax-highlighting via the language class, so failing to handle escapes can produce incorrect class names (breaking highlighting) for language identifiers that include punctuation (e.g., C++). It’s not as ubiquitous as basic emphasis or links, but it’s an important, real-world interoperability issue."
  },
  {
    "section": "Entity and numeric character references",
    "example": 25,
    "weight": 0.7,
    "explanation": "HTML entity and numeric character reference handling is common (e.g. &amp;, &nbsp;, &copy;) and mis-rendering would produce visible entity text or wrong characters, which is confusing; it's important but not as fundamental as headings or code blocks."
  },
  {
    "section": "Entity and numeric character references",
    "example": 26,
    "weight": 0.9,
    "explanation": "Entity and numeric character references are common in real-world HTML/Markdown (for special characters, HTML-escaping, and interoperability); incorrect handling would frequently produce visible raw entities or wrong characters (including failure to replace U+0000 with �), which is confusing and breaks interoperability, so support is highly important."
  },
  {
    "section": "Entity and numeric character references",
    "example": 27,
    "weight": 0.5,
    "explanation": "Numeric (and named) character references appear occasionally in real-world content (HTML-escaped text, copy-pasted snippets, or when inserting specific Unicode characters) and should be recognized outside code spans; if not, readers will see raw &#...; sequences which is confusing but this is less fundamental than core features like headings, emphasis, links, or code blocks."
  },
  {
    "section": "Entity and numeric character references",
    "example": 28,
    "weight": 0.7,
    "explanation": "HTML/entity references are fairly common in real-world Markdown (e.g., &nbsp;, numeric refs) and incorrect handling can change visible text or silently produce wrong characters; while not as fundamental as headings/emphasis, proper entity parsing (and treating invalid/non-semicolon sequences as literal) is important for correct and safe rendering."
  },
  {
    "section": "Entity and numeric character references",
    "example": 29,
    "weight": 0.5,
    "explanation": "Named entities without the trailing semicolon (e.g. `&copy`) are an occasional real‑world occurrence (especially in HTML-ish text), and misrecognizing them can change visible text, but this is not a foundational structural feature — it’s a moderate edge case in entity handling."
  },
  {
    "section": "Entity and numeric character references",
    "example": 30,
    "weight": 0.8,
    "explanation": "Proper handling of entity references and escaping ampersands is common and important for producing valid HTML; mis-parsing unknown entities or leaving raw '&' can produce invalid HTML or incorrect rendering of text (e.g., breaking URLs or displaying wrong characters)."
  },
  {
    "section": "Entity and numeric character references",
    "example": 31,
    "weight": 0.5,
    "explanation": "HTML entity and numeric character references appear reasonably often (e.g., &amp;, &eacute;, &copy;) and incorrect handling results in visible literal entities instead of the intended characters (including in attribute values), which is confusing but not as critical as core structural features like headings or code blocks."
  },
  {
    "section": "Entity and numeric character references",
    "example": 32,
    "weight": 0.7,
    "explanation": "Entity references in link destinations and titles are not rare (especially for accented characters) and affect link targets and visible text; incorrect handling can produce broken URLs or wrong visible characters, so this is an important feature to get right."
  },
  {
    "section": "Entity and numeric character references",
    "example": 33,
    "weight": 0.8,
    "explanation": "Recognizing HTML entities in link destinations and titles is fairly common (especially in content converted from HTML or using legacy entity encodings); if a renderer fails to decode them it can produce broken or incorrect URLs and ugly/incorrect link titles, which is confusing for readers and can break navigation. This is not as fundamental as headings/emphasis but is an important interoperability feature."
  },
  {
    "section": "Entity and numeric character references",
    "example": 34,
    "weight": 0.3,
    "explanation": "Using HTML entities inside a fenced code-block info string (language identifier) is uncommon; failure to decode them would mainly affect the generated language class (and thus syntax highlighting) but not the readable content, so it is a niche but noticeable issue rather than a critical one."
  },
  {
    "section": "Entity and numeric character references",
    "example": 35,
    "weight": 0.8,
    "explanation": "Code spans and blocks are commonly used to show literal text (HTML snippets, entities like &amp;lt; or &amp;amp;), so treating entity references inside them as plain text is important; incorrect decoding would frequently misrepresent examples and confuse readers, though it’s a narrower edge-case than basic block structure."
  },
  {
    "section": "Entity and numeric character references",
    "example": 36,
    "weight": 1.0,
    "explanation": "Treating entity references literally inside code spans/blocks is critical: many docs and READMEs include HTML examples or snippets with entities, and incorrectly decoding them would change the visible source code and mislead readers."
  },
  {
    "section": "Entity and numeric character references",
    "example": 37,
    "weight": 0.5,
    "explanation": "Numeric/entity references appear reasonably often (HTML-encoded content, autogenerated text, or authors using entities to avoid Markdown parsing), and misinterpreting them as emphasis delimiters would change inline formatting and confuse readers; however this is not as central as headings or code blocks, so it's moderately important rather than critical."
  },
  {
    "section": "Entity and numeric character references",
    "example": 38,
    "weight": 0.7,
    "explanation": "HTML entity/numeric references (e.g. &amp;, &#42;) are common in pasted/escaped content and should be decoded in inline text; failing to do so — or treating an entity as a structural delimiter (turning '&#42;' into a list marker) — would produce confusing, incorrect output even though it's not as fundamental as headings or code blocks."
  },
  {
    "section": "Entity and numeric character references",
    "example": 39,
    "weight": 0.5,
    "explanation": "Handling entity and numeric character references is fairly important because named entities like &amp; and &lt; are common, but this exact case (using a numeric reference for a newline, e.g. &#10;) is fairly uncommon. If rendered incorrectly it can show raw codes or subtly change spacing/paragraph breaks, but it is not as critical as basic block-level or inline features."
  },
  {
    "section": "Entity and numeric character references",
    "example": 40,
    "weight": 0.8,
    "explanation": "Entity and numeric character references (e.g. &amp;, &lt;, &nbsp;, &#160;) are commonly used and must be decoded outside code spans; failing to do so would leave raw entities in text and break many documents. The specific decimal reference for a tab is uncommon, but it exercises the same widely used feature, so incorrect handling would be noticeably wrong."
  },
  {
    "section": "Entity and numeric character references",
    "example": 41,
    "weight": 0.4,
    "explanation": "This is a somewhat uncommon edge case: authors rarely use entity references in place of the quote characters that delimit link titles, but mis-parsing it can change plain text into a link (or vice versa) and thus be surprising; it's not foundational but still worth correct handling."
  },
  {
    "section": "Precedence",
    "example": 42,
    "weight": 0.8,
    "explanation": "Block-vs-inline precedence is a foundational parsing rule: lists are very common and mis-parsing this case could turn a normal two-item list into a single broken code span, misleading readers. The exact backtick-across-lines pattern is somewhat uncommon, but failures here indicate a broader parsing bug that would affect many real-world documents."
  },
  {
    "section": "Thematic breaks",
    "example": 43,
    "weight": 0.7,
    "explanation": "Thematic breaks (horizontal rules) are fairly common for visually separating sections and lists, so incorrect handling would be noticeable and could change document structure; however they are not as foundational as headings or links. Edge cases like setext-heading/list precedence increase the chance of confusing render differences."
  },
  {
    "section": "Thematic breaks",
    "example": 44,
    "weight": 0.7,
    "explanation": "Thematic breaks (horizontal rules) are a common structural element and misrecognizing which characters form them would produce noticeable layout and meaning differences; the specific case of '+' is less common than '-', '*' or '_', but getting this rule wrong would still be a visible and confusing deviation from CommonMark."
  },
  {
    "section": "Thematic breaks",
    "example": 45,
    "weight": 1.0,
    "explanation": "Thematic breaks (horizontal rules) are common in real-world Markdown to separate sections; failing to render them or misinterpreting their precedence (vs setext headings or list items) would frequently confuse readers and break document structure."
  },
  {
    "section": "Thematic breaks",
    "example": 46,
    "weight": 0.7,
    "explanation": "Horizontal rules (thematic breaks) are a common structural element; this example tests the required minimum of three matching characters. If a renderer treated two characters as a thematic break it would unexpectedly alter document structure and confuse readers, so correct behavior is important though not as foundational as headings or links."
  },
  {
    "section": "Thematic breaks",
    "example": 47,
    "weight": 0.7,
    "explanation": "Horizontal rules (thematic breaks) are commonly used in READMEs, docs, and chat to separate sections; incorrect or missing rendering would noticeably harm document structure and readability. They are not as foundational as headings or links but are a frequent, important formatting element."
  },
  {
    "section": "Thematic breaks",
    "example": 48,
    "weight": 0.9,
    "explanation": "This tests the precedence between indented code blocks and thematic breaks—both are common Markdown features; getting this wrong can turn code into a horizontal rule (losing or misrepresenting content), so it's highly impactful."
  },
  {
    "section": "Thematic breaks",
    "example": 49,
    "weight": 1.0,
    "explanation": "Thematic breaks (horizontal rules) are a common, foundational Markdown feature used frequently in READMEs, docs and messages; getting them or their precedence rules (indentation, setext headings, lists) wrong would noticeably break document structure and confuse readers."
  },
  {
    "section": "Thematic breaks",
    "example": 50,
    "weight": 0.7,
    "explanation": "Thematic breaks (horizontal rules) are a common structural element in real-world Markdown (section dividers, README separation) and misrendering would noticeably harm document readability and structure, though they are less critical than headings or links."
  },
  {
    "section": "Thematic breaks",
    "example": 51,
    "weight": 0.9,
    "explanation": "Thematic breaks (---, ***, ___ and variants like `- - -`) are a common, foundational block-level feature used frequently to separate sections in READMEs, docs and posts; incorrect handling would noticeably disrupt document structure and reader comprehension."
  },
  {
    "section": "Thematic breaks",
    "example": 52,
    "weight": 1.0,
    "explanation": "Thematic breaks (horizontal rules) are a common, foundational block-level feature used frequently to separate sections in READMEs, docs, and messages; incorrect or missing rendering would noticeably break document structure and confuse readers."
  },
  {
    "section": "Thematic breaks",
    "example": 53,
    "weight": 0.7,
    "explanation": "Thematic breaks (horizontal rules) are fairly common and the precedence rules (vs. list items or setext headings) affect document structure; mis-rendering can produce misleading nesting or missing separators. This is important but not as critical as core elements like headings, paragraphs, links, or code blocks."
  },
  {
    "section": "Thematic breaks",
    "example": 54,
    "weight": 0.7,
    "explanation": "Thematic breaks (HRs) are a common way to separate sections and appear fairly often in READMEs, docs, and chat, and mishandling their syntax or precedence (with lists or setext headings) can produce confusing structure — important to get right, though not as critical as core inline elements like links or strong emphasis."
  },
  {
    "section": "Thematic breaks",
    "example": 55,
    "weight": 1.0,
    "explanation": "Thematic breaks (horizontal rules) are a common, foundational Markdown feature; misrecognizing lines that contain other characters as breaks (e.g., '_ _ _ _ a', 'a------', '---a---') would frequently change document structure and confuse readers."
  },
  {
    "section": "Thematic breaks",
    "example": 56,
    "weight": 0.6,
    "explanation": "Moderately important: the pattern (e.g. \"*-*\") occurs occasionally in readmes, chat and AI output, and misrendering it as a thematic break (HR) instead of inline emphasis would be confusing and destructive, though it's not as fundamental as headings or links."
  },
  {
    "section": "Thematic breaks",
    "example": 57,
    "weight": 0.7,
    "explanation": "Thematic breaks (---, ***, ___) are common in READMEs, docs and chat output as section separators; incorrect handling can change document structure (e.g., becoming a setext heading or list item) and confuse readers, so support is important though not as foundational as headings or links."
  },
  {
    "section": "Thematic breaks",
    "example": 58,
    "weight": 0.7,
    "explanation": "Horizontal rules (thematic breaks) are a common structural element in READMEs, docs and posts; incorrect handling (including indentation limits, mixed characters, or precedence vs setext headings/lists) would noticeably break document layout and readability, though they are slightly less critical than core elements like headings or links."
  },
  {
    "section": "Thematic breaks",
    "example": 59,
    "weight": 1.0,
    "explanation": "Critical — thematic breaks (and their precedence over setext underlines) are a fundamental Markdown construct that appears frequently; getting this wrong can change document structure (heading vs rule) and confuse readers."
  },
  {
    "section": "Thematic breaks",
    "example": 60,
    "weight": 0.7,
    "explanation": "Thematic breaks (horizontal rules) are frequently used in READMEs, docs, and posts to separate sections; getting them wrong (or misparsing them as headings or list items) alters document structure and visual flow and would be noticeably confusing to readers."
  },
  {
    "section": "Thematic breaks",
    "example": 61,
    "weight": 1.0,
    "explanation": "Thematic breaks (horizontal rules) are a common, foundational Markdown feature used frequently in READMEs, docs, blogs and chat; incorrect handling (including interactions with lists and setext headings) changes document structure and can be confusing, so correct support is critical."
  },
  {
    "section": "ATX headings",
    "example": 62,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used widely in READMEs, docs, and chat; incorrect or missing support would frequently break document structure and confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 63,
    "weight": 0.3,
    "explanation": "This tests an edge-case of ATX headings (>6 # not being a heading). Headings themselves are critical, but the specific >6 rule is rare in real content; incorrect rendering would be surprising but not commonly encountered."
  },
  {
    "section": "ATX headings",
    "example": 64,
    "weight": 0.9,
    "explanation": "This affects correct recognition of ATX headings versus common text like #hashtags or #5 (e.g., issue numbers, tags, or social text); it's a foundational parsing rule whose misrendering frequently changes document structure and is therefore highly impactful."
  },
  {
    "section": "ATX headings",
    "example": 65,
    "weight": 0.6,
    "explanation": "This tests backslash-escaped leading # so the line is rendered as literal text rather than a heading; while core ATX headings are foundational, correct handling of escapes is important for Markdown tutorials, READMEs and examples and misrendering would be confusing, though it's a somewhat specialized case."
  },
  {
    "section": "ATX headings",
    "example": 66,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used ubiquitously in documentation, READMEs, and chat; failing to support them (including inline parsing and escapes within headings) would break document structure and cause significant confusion."
  },
  {
    "section": "ATX headings",
    "example": 67,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used ubiquitously in docs, READMEs, and chat; incorrect handling of spacing, optional closing #'s, or empty headings would frequently break structure and confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 68,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used pervasively in READMEs, docs and chat; incorrect rendering would significantly break document structure and readability, so lack of correct support is critical."
  },
  {
    "section": "ATX headings",
    "example": 69,
    "weight": 1.0,
    "explanation": "ATX headings are fundamental and ubiquitous in Markdown (titles, section headers); incorrect or missing support would severely damage document structure and readability — subtle rules (spaces, closing hashes, indentation) also commonly affect real-world rendering."
  },
  {
    "section": "ATX headings",
    "example": 70,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational, very common Markdown feature (headings, closing hashes, indentation, escaping and empty headings) and incorrect rendering would frequently confuse readers and break document structure."
  },
  {
    "section": "ATX headings",
    "example": 71,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used very frequently (titles, section headings, TOCs); incorrect handling (levels, required spaces, trailing # behavior, indentation limits, escaping) would markedly break document structure and readability in most real-world content."
  },
  {
    "section": "ATX headings",
    "example": 72,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used ubiquitously in READMEs, docs, blogs and chat; incorrect or missing support would severely break document structure and readability. Many subtle rules (levels, spacing, escaping, closing hashes) matter for correct parsing, so failures are highly impactful."
  },
  {
    "section": "ATX headings",
    "example": 73,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used ubiquitously in READMEs, documentation and chat; incorrect handling of spacing, closing hashes, indentation or escaping would frequently break document structure and confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 74,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used pervasively; incorrect handling of opening/closing '#' sequences (such as misparsing '### foo ### b') would commonly break document structure and confuse readers, so correct rendering is critical."
  },
  {
    "section": "ATX headings",
    "example": 75,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational Markdown feature used pervasively; incorrect handling of opening/closing # sequences (including whether a trailing # is part of the content) would break document structure and frequently confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 76,
    "weight": 1.0,
    "explanation": "ATX headings are a fundamental, very common Markdown feature (headings appear in virtually all docs/READMEs); incorrect handling of opening/closing #, required space, or escapes would frequently break document structure and confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 77,
    "weight": 1.0,
    "explanation": "ATX headings are a foundational feature used ubiquitously in READMEs, docs, chat and AI output; incorrect handling (levels, required spaces, closing hashes, indentation, escaping) would severely break document structure and reader comprehension."
  },
  {
    "section": "ATX headings",
    "example": 78,
    "weight": 1.0,
    "explanation": "ATX headings are a fundamental Markdown feature used pervasively in READMEs, docs, and chat; incorrect handling of heading delimiters, spacing, levels, or escaping would frequently break document structure and confuse readers."
  },
  {
    "section": "ATX headings",
    "example": 79,
    "weight": 0.3,
    "explanation": "Empty ATX headings are a valid but uncommon edge case; most documents use non-empty headings, so failing to render empty headings correctly is unlikely to be highly disruptive, though it is a spec compliance detail."
  },
  {
    "section": "Setext headings",
    "example": 80,
    "weight": 0.9,
    "explanation": "Setext headings are a common, foundational heading form (especially in README files and older docs); failing to support them or to parse inline content inside them would frequently break document structure and confuse readers even though ATX headers are an alternative."
  },
  {
    "section": "Setext headings",
    "example": 81,
    "weight": 0.3,
    "explanation": "Multiline setext headings (especially with inline markup spanning lines) are relatively rare in real-world docs and many renderers already differ here; incorrect rendering would be noticeable but not critical compared with core features like headings, lists, or links."
  },
  {
    "section": "Setext headings",
    "example": 82,
    "weight": 0.5,
    "explanation": "Setext headings are fairly common, but multiline setext headings and inline emphasis spanning lines are relatively uncommon and many renderers already handle them differently; incorrect rendering can change block structure and inline emphasis (which is confusing) but is not as critical as core features like basic headings, links, or code blocks."
  },
  {
    "section": "Setext headings",
    "example": 83,
    "weight": 0.9,
    "explanation": "Setext headings are a core heading syntax found in many READMEs and documentation; failing to render them correctly would commonly misrepresent document structure (turning headings into paragraphs or thematic breaks) and confuse readers."
  },
  {
    "section": "Setext headings",
    "example": 84,
    "weight": 1.0,
    "explanation": "Setext headings are a fundamental heading syntax used frequently in READMEs and other Markdown documents; incorrect handling (wrong heading level, merging paragraphs, or mis-recognizing thematic breaks) would materially break document structure and be highly confusing to readers."
  },
  {
    "section": "Setext headings",
    "example": 85,
    "weight": 0.5,
    "explanation": "This tests the interaction of indented code blocks with setext heading parsing — both headings and code blocks are foundational, and getting this wrong can be confusing, but the exact pattern (indented code followed immediately by a setext underline) is relatively uncommon in real-world docs, making it a moderate-impact edge case."
  },
  {
    "section": "Setext headings",
    "example": 86,
    "weight": 1.0,
    "explanation": "Setext headings are a basic, widely used heading syntax in Markdown (especially for level 1/2 headings) — failing to support or incorrectly render an underlined heading (including up to 3-space indentation and trailing spaces) would break common documents and confuse readers, so this is a critical feature."
  },
  {
    "section": "Setext headings",
    "example": 87,
    "weight": 0.5,
    "explanation": "This is a moderately important edge case: it tests the 3-vs-4-space indentation rule for setext underlines and its interaction with code blocks/thematic breaks. Misrendering can change document structure (heading vs code/paragraph), but the specific pattern is relatively uncommon in everyday Markdown."
  },
  {
    "section": "Setext headings",
    "example": 88,
    "weight": 0.5,
    "explanation": "Setext headings are a common Markdown feature (especially in READMEs), but the specific rule about underlines not containing internal spaces is a somewhat uncommon edge case; getting it wrong can produce misleading headings or thematic breaks, but it won't break the majority of documents."
  },
  {
    "section": "Setext headings",
    "example": 89,
    "weight": 0.6,
    "explanation": "Setext headings are a common alternate heading syntax and mis-parsing them (or treating trailing spaces as a hard break so the underline no longer forms a heading) will often turn intended headings into paragraphs or thematic breaks, which is moderately disruptive to readability even if not as ubiquitous as ATX headings."
  },
  {
    "section": "Setext headings",
    "example": 90,
    "weight": 0.6,
    "explanation": "Setext headings are a core Markdown feature and incorrect handling would be confusing in many docs, but the specific behavior here (a trailing backslash being preserved rather than creating a hard break) is a relatively rare edge case, so the impact is moderate-high rather than critical."
  },
  {
    "section": "Setext headings",
    "example": 91,
    "weight": 0.3,
    "explanation": "Setext headings are a common feature, but this example exercises a rare edge-case (multiline heading content interacting with backticks and broken HTML tags). Most real-world documents won't rely on this exact pattern, so incorrect rendering would be confusing in specific cases but is not broadly critical."
  },
  {
    "section": "Setext headings",
    "example": 92,
    "weight": 0.7,
    "explanation": "This tests the interaction between blockquotes and setext/thematic-break rules — blockquotes and horizontal rules are common, and getting this wrong can change structure (turning a blockquote into a heading or putting the HR inside the quote) which is confusing. It's not as foundational as headings themselves but is an important, fairly frequent block-structure behavior. "
  },
  {
    "section": "Setext headings",
    "example": 93,
    "weight": 0.3,
    "explanation": "This is a relatively niche edge case — setext underlines used as lazy continuation inside block quotes are uncommon in real-world content. Misrendering could be mildly confusing within the blockquote, but it doesn't affect core Markdown features and is not widely encountered."
  },
  {
    "section": "Setext headings",
    "example": 94,
    "weight": 0.6,
    "explanation": "This is a moderately important interplay between lists and setext/thmatic-break parsing: authors sometimes place lines of dashes after list items (intentionally as an <hr>), and mis-parsing could turn list items into headings or otherwise change document structure — not extremely common but likely to cause confusing, structural errors in many real-world docs."
  },
  {
    "section": "Setext headings",
    "example": 95,
    "weight": 0.5,
    "explanation": "Setext headings are a common Markdown heading form, but the multiline setext variant tested here is relatively uncommon and many renderers already differ; incorrect handling can change document structure (heading vs paragraph+thematic break) and confuse readers, but it is not as critical as basic heading/paragraph support."
  },
  {
    "section": "Setext headings",
    "example": 96,
    "weight": 0.7,
    "explanation": "This example exercises the interaction between thematic breaks and setext heading underlines — misinterpreting it can turn headings into <hr> or vice versa and change document structure, which is important and fairly likely to occur in real documents even if the exact pattern is somewhat niche."
  },
  {
    "section": "Setext headings",
    "example": 97,
    "weight": 0.3,
    "explanation": "A lone line of '=' should not form a setext heading and is an uncommon pattern in real-world documents; misrendering it as a heading would be confusing but is a relatively niche edge case compared with core features like headings, lists, or links."
  },
  {
    "section": "Setext headings",
    "example": 98,
    "weight": 1.0,
    "explanation": "Lines of three or more dashes used as thematic breaks (---) are extremely common and foundational; mis-rendering them as setext heading underlines or otherwise would frequently break document structure and be confusing to readers."
  },
  {
    "section": "Setext headings",
    "example": 99,
    "weight": 0.6,
    "explanation": "This is a moderately important interaction: list items and dashed lines are common in real docs, and misinterpreting a lazy setext underline inside a list (turning it into a heading or merging content) would change structure and be confusing. It's not as fundamental as basic headings or links, but the list/thematic-break interplay occurs often enough to matter."
  },
  {
    "section": "Setext headings",
    "example": 100,
    "weight": 0.5,
    "explanation": "This is a moderately important edge case: misinterpreting a 4-space-indented line as a paragraph (allowing the following --- to become a setext underline) can turn code into headings or merge blocks, which is confusing, but the exact pattern (indented code immediately followed by a setext-like underline) is less common than core features like headings, links, or fenced code blocks."
  },
  {
    "section": "Setext headings",
    "example": 101,
    "weight": 0.4,
    "explanation": "This is an edge-case interaction (setext underline vs lazy continuation in block quotes). Misrendering can change block structure (turning a blockquote into a heading or wrapping an HR incorrectly), which can be confusing, but the exact pattern is relatively uncommon in typical documents."
  },
  {
    "section": "Setext headings",
    "example": 102,
    "weight": 0.5,
    "explanation": "Setext headings are a basic heading form but less common than ATX; the backslash-escape to prevent a blockquote marker is a somewhat specific case. Incorrect rendering could change block structure (blockquote vs heading) and be confusing, but it is not as ubiquitous or critical as core features like basic headings, links, or code blocks."
  },
  {
    "section": "Setext headings",
    "example": 103,
    "weight": 0.7,
    "explanation": "Setext headings are a common Markdown feature (especially in READMEs and docs) and mis-parsing the blank-line rule can change document structure (turning paragraphs into headings or vice versa), which would be confusing; it's not as ubiquitous as ATX headings but is still important."
  },
  {
    "section": "Setext headings",
    "example": 104,
    "weight": 0.7,
    "explanation": "Setext headings and thematic breaks are fairly common and this rule changes document structure (heading vs. paragraph + hr); misrendering would frequently produce misleading headings or missing horizontal rules, so it's important though not as foundational as ATX headings."
  },
  {
    "section": "Setext headings",
    "example": 105,
    "weight": 0.5,
    "explanation": "Setext headings and thematic breaks are common Markdown constructs, but the specific multiline setext vs. thematic-break interaction is an occasional edge case; incorrect rendering can be confusing but won’t break most documents that use ATX headings or explicitly separated blocks."
  },
  {
    "section": "Setext headings",
    "example": 106,
    "weight": 0.5,
    "explanation": "Setext headings themselves are common enough, but this exact case — using a backslash escape so a line of dashes is treated as literal text rather than a setext underline/thematic break — is an occasional edge case. Getting it wrong can change paragraph/heading structure and confuse readers, but it is not as ubiquitous or foundational as basic headings, links, or code blocks."
  },
  {
    "section": "Indented code blocks",
    "example": 107,
    "weight": 1.0,
    "explanation": "Indented code blocks are a foundational way to represent code/monospaced blocks in Markdown (commonly found in READMEs, emails, nested lists and legacy docs); failing to render them correctly would frequently misrepresent code and structure, causing significant confusion."
  },
  {
    "section": "Indented code blocks",
    "example": 108,
    "weight": 1.0,
    "explanation": "This is a common, foundational interaction between lists and indented code blocks — many real-world documents use hanging indents or multi-paragraph list items; rendering them as code or breaking the list would be highly confusing and break typical Markdown usage."
  },
  {
    "section": "Indented code blocks",
    "example": 109,
    "weight": 1.0,
    "explanation": "Nested lists vs. indented code blocks are a common, foundational pattern in Markdown; getting this wrong (rendering a nested list as a code block) would frequently break document structure and meaning and be very confusing to readers."
  },
  {
    "section": "Indented code blocks",
    "example": 110,
    "weight": 1.0,
    "explanation": "Indented code blocks are a foundational way to denote preformatted/code content in Markdown and still appear commonly (especially in legacy docs, emails, and simple notes); failing to render them correctly would frequently break meaning and confuse readers."
  },
  {
    "section": "Indented code blocks",
    "example": 111,
    "weight": 1.0,
    "explanation": "Indented code blocks are a fundamental way to represent preformatted/code content in Markdown; many real-world docs and READMEs still use them and misrendering would seriously confuse or corrupt code and formatting, so correct support is critical."
  },
  {
    "section": "Indented code blocks",
    "example": 112,
    "weight": 1.0,
    "explanation": "Indented code blocks are a fundamental way to represent code in Markdown (still common in legacy README files, nested-list contexts, and many documents), and getting them wrong would misrepresent code formatting and meaning, so correct rendering is critical."
  },
  {
    "section": "Indented code blocks",
    "example": 113,
    "weight": 0.9,
    "explanation": "Indented code blocks are a fundamental way to show code or preformatted text in Markdown and are still common in READMEs, documentation and AI output; failing to render them would often merge code with prose and confuse readers. While fenced code blocks are increasingly used, many real-world documents and legacy content rely on indented blocks, so correct handling is important."
  },
  {
    "section": "Indented code blocks",
    "example": 114,
    "weight": 1.0,
    "explanation": "Indented code blocks are a fundamental way to denote code in Markdown (widely used in READMEs, docs, and examples); failing to render them correctly would frequently break meaning and readability and is therefore critical to get right."
  },
  {
    "section": "Indented code blocks",
    "example": 115,
    "weight": 1.0,
    "explanation": "Indented code blocks are a fundamental way to represent code and block-level content in Markdown; many real-world documents still use them and incorrect handling (including their interactions with headings, paragraphs, and lists) would significantly alter meaning and confuse readers."
  },
  {
    "section": "Indented code blocks",
    "example": 116,
    "weight": 1.0,
    "explanation": "Indented code blocks are a fundamental Markdown feature used frequently for code/examples; rendering them incorrectly (losing preserved whitespace or treating them as normal text) would regularly confuse readers and break documentation."
  },
  {
    "section": "Indented code blocks",
    "example": 117,
    "weight": 1.0,
    "explanation": "Code blocks are a fundamental Markdown feature and indented code blocks are still common (especially in older docs, emails, and some generators); failing to support them would frequently break code display and seriously confuse readers."
  },
  {
    "section": "Indented code blocks",
    "example": 118,
    "weight": 1.0,
    "explanation": "Indented code blocks are a foundational Markdown feature for showing preformatted code/text; they appear frequently (especially in legacy docs, READMEs, and generated output), and failing to render them correctly would severely harm readability and meaning."
  },
  {
    "section": "Fenced code blocks",
    "example": 119,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational Markdown feature used extremely frequently (README files, docs, chat, AI output); incorrect handling (e.g., not preserving literal content or failing to escape HTML) would be widely visible and highly misleading."
  },
  {
    "section": "Fenced code blocks",
    "example": 120,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a fundamental, frequently used Markdown feature (READMEs, docs, chat, AI output); incorrect handling of fences, info strings, or literal content would seriously break code formatting and confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 121,
    "weight": 1.0,
    "explanation": "Fenced code blocks are ubiquitous in README files, docs, chat and AI output; mishandling backtick/tilde fences or info-string rules would frequently break code display and seriously confuse readers, so correct support is critical."
  },
  {
    "section": "Fenced code blocks",
    "example": 122,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational Markdown feature; requiring the same fence character is essential — getting this wrong can prematurely terminate code blocks (or fail to close them) and will frequently break real-world code samples and README content."
  },
  {
    "section": "Fenced code blocks",
    "example": 123,
    "weight": 1.0,
    "explanation": "Fenced code blocks are ubiquitous in READMEs, docs, chats and AI outputs and are a foundational feature (including info strings for language highlighting); incorrect handling would frequently break code display and confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 124,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational, ubiquitous feature in READMEs, documentation, and chat/code snippets; incorrect handling of fence characters, lengths, or info strings would frequently break code display and confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 125,
    "weight": 1.0,
    "explanation": "Fenced code block rules are foundational: many real-world documents include code or markdown that contains backtick/tilde sequences, and mishandling fence character/length (or allowing a shorter fence inside to close the block) will frequently break or corrupt examples and be highly confusing."
  },
  {
    "section": "Fenced code blocks",
    "example": 126,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational feature used pervasively in READMEs, docs, chat/AI outputs and code examples; incorrect handling (including info strings, fence length/char, or indentation rules) will frequently break code display or mis-parse content, causing major confusion."
  },
  {
    "section": "Fenced code blocks",
    "example": 127,
    "weight": 0.7,
    "explanation": "Using longer fences to include backticks (or nested fences) appears reasonably often in documentation and READMEs (especially when showing Markdown examples), and getting this wrong makes examples unreadable or misleading, so it is an important but not baseline feature."
  },
  {
    "section": "Fenced code blocks",
    "example": 128,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a fundamental Markdown feature and commonly occur inside blockquotes (e.g., examples, emails, READMEs); incorrect handling of the blockquote/fence interaction would frequently misplace code or paragraphs and be confusing to readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 129,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational, very common Markdown feature (used in READMEs, docs, chat, AI output); incorrect handling of fences, info strings, or closing rules would frequently break code display and be confusing or misleading to readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 130,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational, ubiquitous feature in READMEs, docs, chat and code sharing; failure to support them correctly would badly break code presentation and frequently confuse or mislead readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 131,
    "weight": 0.7,
    "explanation": "Fenced code blocks are a core Markdown feature and indent-sensitive behavior matters when fences appear inside lists or blockquotes; incorrect handling would frequently produce wrong leading spaces in code content and confuse readers, though it’s a fairly specific detail of fencing rather than the most basic syntax."
  },
  {
    "section": "Fenced code blocks",
    "example": 132,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational Markdown feature; the indentation-removal behavior for indented fences matters for nested lists/blockquotes and for preserving literal code content. Incorrect rendering would frequently misrepresent code examples and confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 133,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a fundamental Markdown feature and correct handling of indentation when fences are indented (e.g., inside lists or blockquotes) is common; getting this wrong will frequently misrepresent code/layout and confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 134,
    "weight": 0.5,
    "explanation": "This is a specific interaction — fenced-code markers appearing inside an indented (4-space) code block should be treated as literal text. It’s not extremely common in everyday docs but does occur in README/spec examples; incorrect rendering would confuse code examples though it’s not a foundational Markdown feature."
  },
  {
    "section": "Fenced code blocks",
    "example": 135,
    "weight": 1.0,
    "explanation": "Fenced code blocks are extremely common (READMEs, chat, docs); incorrectly handling closing fences or indentation would frequently break code rendering (especially in lists/quotes) and be highly confusing to readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 136,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a fundamental and very common feature (READMEs, docs, chat, AI output); incorrect handling of fences, indentation limits, or info strings would frequently break code samples and mislead readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 137,
    "weight": 0.9,
    "explanation": "Fenced code blocks are a foundational feature — misinterpreting a closing fence because of indentation (4 spaces should not close) will commonly truncate or corrupt code examples and is therefore highly impactful in real-world docs and READMEs."
  },
  {
    "section": "Fenced code blocks",
    "example": 138,
    "weight": 0.5,
    "explanation": "Fenced code blocks are a common, important feature, but this example exercises an uncommon corner case (that backtick-based info strings must not contain backticks). Misrendering would be surprising when it occurs but is relatively rare in real-world documents."
  },
  {
    "section": "Fenced code blocks",
    "example": 139,
    "weight": 0.7,
    "explanation": "Fenced code blocks are common, and incorrectly treating a line with extra text after a fence as a closing fence (or vice versa) can cause large portions of a document to be mis-rendered or swallowed; this specific trailing-content rule is not the most common case but is important to get right to avoid confusing output."
  },
  {
    "section": "Fenced code blocks",
    "example": 140,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational, very commonly used feature in READMEs, documentation, chat, and AI output; incorrect handling would frequently break code snippets, alter document structure, and significantly confuse readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 141,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a fundamental, commonly used Markdown feature; getting their parsing (including interaction with surrounding headings and lack of required blank lines) wrong would frequently break real-world documents and be highly confusing to readers."
  },
  {
    "section": "Fenced code blocks",
    "example": 142,
    "weight": 1.0,
    "explanation": "Fenced code blocks are a foundational feature used extensively in READMEs, docs, chat and AI output; incorrect rendering would frequently break code presentation, syntax highlighting, and reader understanding, so lack of correct support is critical."
  },
  {
    "section": "Fenced code blocks",
    "example": 143,
    "weight": 1.0,
    "explanation": "Fenced code blocks (including info strings for language) are a foundational feature used pervasively in READMEs, docs and chats; incorrect handling (closing-fence rules or language parsing) would frequently break code rendering and syntax highlighting, causing significant confusion."
  },
  {
    "section": "Fenced code blocks",
    "example": 144,
    "weight": 0.5,
    "explanation": "Fenced code blocks and info strings are common, but the specific rule that backtick-based fences cannot contain backticks in the info string is an edge case; incorrect handling is uncommon but can break block detection and corrupt content, so it is moderately important."
  },
  {
    "section": "Fenced code blocks",
    "example": 145,
    "weight": 0.5,
    "explanation": "This is a moderately important parsing edge case: it determines whether a sequence is treated as an inline code span or a fenced code block (info strings for backtick fences must not contain backticks). It doesn't occur extremely often, but getting it wrong produces clearly confusing output, so correct handling is moderately useful."
  },
  {
    "section": "Fenced code blocks",
    "example": 146,
    "weight": 0.3,
    "explanation": "This is a niche edge case: most authors use simple info strings (and backtick fences), so allowing backticks/tildes inside a tilde-fence info string is uncommon; incorrect rendering would be confusing in those rare cases but is not broadly impactful."
  },
  {
    "section": "Fenced code blocks",
    "example": 147,
    "weight": 0.7,
    "explanation": "Fenced code blocks are widely used and this tests a common subtlety: backtick lines inside a backtick fence must be treated as content (info strings for backtick fences cannot contain backticks). Misrendering breaks many documentation/README examples that show Markdown fences and is therefore important though somewhat specialized."
  },
  {
    "section": "HTML blocks",
    "example": 148,
    "weight": 0.7,
    "explanation": "Raw HTML blocks (and their interactions with blank lines and literal tags like <pre>, <script>, <style>) appear often in real-world Markdown (embedded HTML, READMEs, pasted snippets) and mis-handling them produces surprising, semantically wrong output (broken code blocks, lost formatting). This is not as fundamental as headings/links but is important enough to noticeably confuse readers."
  },
  {
    "section": "HTML blocks",
    "example": 149,
    "weight": 0.8,
    "explanation": "Treating block-level HTML (like <table>) as raw HTML until a blank line is a common need — people paste raw HTML tables and other block tags in READMEs and docs. If a renderer mishandles this (escaping tags, ending the block too early, or merging it with following text) the layout will be broken or confusing, so correct handling is important though not as universally foundational as headings/emphasis."
  },
  {
    "section": "HTML blocks",
    "example": 150,
    "weight": 0.7,
    "explanation": "This tests HTML block handling where a block-level tag (with up to three leading spaces) starts a raw HTML block so lines like `*hello*` remain verbatim; embedding HTML is fairly common and mis-rendering would produce visible, confusing formatting errors, so correct behavior is important."
  },
  {
    "section": "HTML blocks",
    "example": 151,
    "weight": 0.7,
    "explanation": "Handling of HTML blocks (including lines that are just closing tags) is fairly common in docs that mix HTML and Markdown; getting this wrong can cause following Markdown to be swallowed or HTML to be escaped, which is confusing, so it is important though not as universally critical as core elements like headings or lists."
  },
  {
    "section": "HTML blocks",
    "example": 152,
    "weight": 0.9,
    "explanation": "Using block-level HTML (e.g. <div class=\"...\">) to add attributes or layout around Markdown is common in READMEs and web docs; if a renderer mishandles these HTML block rules the tags may be escaped or the inner Markdown not parsed, producing confusing or broken output."
  },
  {
    "section": "HTML blocks",
    "example": 153,
    "weight": 0.5,
    "explanation": "Embedding raw HTML is fairly common and attributes wrapped to a new line do occur (e.g., long attributes or pretty-printed HTML). Misparsing this as normal Markdown or splitting the block would produce confusing output, but this is not a core Markdown feature used as often as headings/links, so the impact is moderate."
  },
  {
    "section": "HTML blocks",
    "example": 154,
    "weight": 0.7,
    "explanation": "Splitting tag attributes across lines is common in pretty‑printed or pasted HTML; incorrect handling would break raw-HTML passthrough and produce confusing/malformed output. It's not as fundamental as headings or lists but is an important real-world interoperability case."
  },
  {
    "section": "HTML blocks",
    "example": 155,
    "weight": 0.7,
    "explanation": "HTML block handling (e.g., <div> starting a raw-HTML block that ends at a blank line) is fairly common and important: getting it wrong can cause Markdown inside such blocks to be wrongly parsed or passed through, leading to confusing rendering and broken layouts."
  },
  {
    "section": "HTML blocks",
    "example": 156,
    "weight": 0.5,
    "explanation": "Splitting an HTML start tag across lines and having the following lines treated as part of an HTML block is a moderately uncommon but real pattern when embedding raw HTML; if rendered incorrectly it can turn intended raw HTML into parsed Markdown (e.g. emphasizing *hi*), which is confusing, but it’s not as foundational as headings or links."
  },
  {
    "section": "HTML blocks",
    "example": 157,
    "weight": 0.5,
    "explanation": "This checks a somewhat common HTML-block nuance (a tag start split across lines) — misinterpreting it can cause content to be treated as raw HTML or as normal Markdown incorrectly, which is confusing but not catastrophic for most documents."
  },
  {
    "section": "HTML blocks",
    "example": 158,
    "weight": 0.8,
    "explanation": "This tests HTML block detection (a line starting with '<div ' should start a raw HTML block so subsequent lines are passed through verbatim). Embedding HTML blocks is fairly common, and mis-handling this (e.g., parsing *foo* as emphasis) can produce surprising, misleading output, so it's important though not as universally critical as core elements like headings or links."
  },
  {
    "section": "HTML blocks",
    "example": 159,
    "weight": 0.7,
    "explanation": "Treating a complete tag on its own line as a raw HTML block (so inner markup like *foo* is not parsed) occurs often enough in real documents and can noticeably change rendering (breaking links, formatting or structure) if handled incorrectly, so it is important though not as fundamental as headings or code blocks."
  },
  {
    "section": "HTML blocks",
    "example": 160,
    "weight": 0.9,
    "explanation": "Raw HTML blocks (especially block-level tags like <table>) are commonly used in real-world docs and READMEs; if a renderer fails to treat the multi-line HTML block correctly it can corrupt document structure (broken tables, unexpected Markdown parsing of inner lines) and be very confusing to readers."
  },
  {
    "section": "HTML blocks",
    "example": 161,
    "weight": 0.8,
    "explanation": "Mixing raw HTML blocks and subsequent Markdown (e.g., a lone <div> line followed by a fenced code block) appears fairly often in READMEs and docs; if a renderer mishandles HTML block termination rules it can swallow or misinterpret large sections of content, producing confusing output, so correct handling is important though slightly less fundamental than basic elements like headings/links."
  },
  {
    "section": "HTML blocks",
    "example": 162,
    "weight": 0.5,
    "explanation": "This exercises the rule that an open tag on its own line creates an HTML block so the inner `*bar*` is passed through verbatim; it’s an uncommon authoring pattern (most people put `<a>text</a>` on one line) but when it does occur misrendering is visibly confusing, so it’s moderately important to get right."
  },
  {
    "section": "HTML blocks",
    "example": 163,
    "weight": 0.6,
    "explanation": "HTML block handling is important because many documents include raw HTML; type‑7 (single-line custom tags) is less common than basic tags but still used. If a renderer misinterprets such a tag it can wrongly parse or escape inner Markdown (changing meaning), so incorrect rendering would be moderately impactful."
  },
  {
    "section": "HTML blocks",
    "example": 164,
    "weight": 0.8,
    "explanation": "Authors frequently embed raw HTML (to add classes, attributes or nonstandard structure) and expect it to be passed through verbatim; if a renderer fails to treat a tag-on-its-own-line as an HTML block, Markdown inside may be interpreted or escaped, changing layout/meaning and causing user-visible breakage."
  },
  {
    "section": "HTML blocks",
    "example": 165,
    "weight": 0.3,
    "explanation": "A solitary closing tag on its own line (type 7 HTML block) is an uncommon pattern; most documents don't rely on this edge-case behavior. Incorrect handling would occasionally surprise users pasting HTML fragments, but it isn't critical for typical Markdown use."
  },
  {
    "section": "HTML blocks",
    "example": 166,
    "weight": 0.7,
    "explanation": "Correct HTML block handling is a common and important feature: many documents include raw HTML (including tags like <del>) and rely on tags-on-their-own-line being treated as raw blocks so inner Markdown is not parsed; getting this wrong can produce visibly incorrect formatting and confuse readers."
  },
  {
    "section": "HTML blocks",
    "example": 167,
    "weight": 0.7,
    "explanation": "Placing a block-level or other tag on its own line to create an HTML block that wraps Markdown content (e.g., <div>, <details>, <del>) is a relatively common pattern; incorrect handling will produce surprising verbatim HTML or lose formatting, so support is important though not as fundamental as headings or code blocks."
  },
  {
    "section": "HTML blocks",
    "example": 168,
    "weight": 0.7,
    "explanation": "This tests whether an inline HTML tag that contains Markdown (e.g. <del>*foo*</del>) is parsed as inline HTML (allowing emphasis) rather than as a raw HTML block. Inline HTML wrapping Markdown appears reasonably often and misrendering would be visibly confusing, so it's an important (but not foundational) behavior to get right."
  },
  {
    "section": "HTML blocks",
    "example": 169,
    "weight": 0.7,
    "explanation": "This tests block-level HTML handling (a <pre>…</pre> HTML block that may contain blank lines and must end at its end tag). Embedded HTML like <pre>/<code> appears often enough in READMEs and legacy content, and getting it wrong can swallow or misclassify following text, so it's important though not as fundamental as basic headings/links."
  },
  {
    "section": "HTML blocks",
    "example": 170,
    "weight": 0.8,
    "explanation": "HTML blocks (especially <script>, <style>, <pre>, and comments) are common in web-oriented docs and can contain blank lines; getting their boundaries wrong can either expose raw HTML as text or swallow/merge subsequent Markdown, which is confusing and often breaks page structure. This is not as universally fundamental as headings/emphasis, but is an important, frequently-encountered feature for correct rendering."
  },
  {
    "section": "HTML blocks",
    "example": 171,
    "weight": 1.0,
    "explanation": "Treating HTML blocks (especially raw-content tags like <pre>, <script>, <style>, <textarea>) as raw is fundamental: these appear in real documents and must not be parsed as Markdown (or functionality and semantics can break), so incorrect handling would be highly disruptive."
  },
  {
    "section": "HTML blocks",
    "example": 172,
    "weight": 0.8,
    "explanation": "Handling of <style>/<script>/<pre>/<textarea> blocks is fairly common in real-world docs and must allow blank lines until a matching end tag; if rendered incorrectly CSS/JS would be mangled or shown as Markdown, producing confusing/broken output. This is an important (though not core like headings) parser behavior."
  },
  {
    "section": "HTML blocks",
    "example": 173,
    "weight": 0.5,
    "explanation": "Multi-line start tags for <style>/<script> (type 1 HTML blocks) appear occasionally in real-world Markdown when embedding raw HTML/CSS/JS; if a renderer fails to treat them as raw HTML it can show or escape the content incorrectly and break page behavior. However, this is less frequent than core features (headings, lists, links) so it is moderately important rather than critical."
  },
  {
    "section": "HTML blocks",
    "example": 174,
    "weight": 0.7,
    "explanation": "This tests raw HTML block behavior (type 6) inside a blockquote — handling of HTML blocks is common enough and getting the end/blank-line rules wrong can move content in or out of the blockquote, producing confusing/misleading output, so it's an important feature to support correctly."
  },
  {
    "section": "HTML blocks",
    "example": 175,
    "weight": 0.5,
    "explanation": "This tests the interaction between HTML block detection and list items — a somewhat uncommon but real pattern that can drastically change document structure if mis-parsed. Incorrect handling is moderately confusing (it can break list structure or leak raw HTML), so it’s useful but not as fundamental as headings/links/code."
  },
  {
    "section": "HTML blocks",
    "example": 176,
    "weight": 0.7,
    "explanation": "Handling of HTML blocks (especially <style>, <script>, <pre>, etc.) is fairly common in real-world Markdown that mixes HTML and Markdown; if a renderer fails to recognize these blocks it can swallow or mis-parse the following Markdown (breaking formatting or producing raw HTML), so incorrect behavior would be noticeably misleading though not as ubiquitous as core features like headings or links."
  },
  {
    "section": "HTML blocks",
    "example": 177,
    "weight": 0.5,
    "explanation": "HTML comment blocks that start at the line beginning are fairly common, and the subtle rule that a comment line (even if closed on the same line) makes the whole line a raw HTML block can change whether following Markdown is parsed. It’s not a core feature like headings or links, but getting this wrong can produce surprising loss or literalification of content, so it’s moderately important."
  },
  {
    "section": "HTML blocks",
    "example": 178,
    "weight": 0.7,
    "explanation": "HTML blocks (especially <script>/<style>/<pre> cases) appear fairly often in web and documentation content; getting the end-tag handling wrong (including text after the end tag in the raw HTML block or mistakenly continuing Markdown parsing) produces confusing, incorrect rendering (e.g., swallowing lists or showing raw markup), so correct support is important."
  },
  {
    "section": "HTML blocks",
    "example": 179,
    "weight": 0.7,
    "explanation": "HTML comments are a common way to include raw HTML or hide content; mis-parsing them (e.g., treating blank lines inside a <!-- --> block as breaking the block) can expose or misformat commented content and break surrounding structure, so correct handling is important though not as ubiquitous as basic elements like headings."
  },
  {
    "section": "HTML blocks",
    "example": 180,
    "weight": 0.7,
    "explanation": "Processing-instruction HTML blocks (e.g. <?php ... ?>) occur fairly often in PHP examples and embedded HTML and, if handled incorrectly, can cause code to be escaped, swallowed, or parsed as normal markdown—leading to confusing or broken output; this is important but not as universally foundational as headings or inline links."
  },
  {
    "section": "HTML blocks",
    "example": 181,
    "weight": 0.5,
    "explanation": "A DOCTYPE declaration is occasionally included when authors embed full HTML in Markdown and relies on HTML-block handling; if a renderer escaped or mis-parsed it the output would be wrong or visibly mangled, but most ordinary Markdown documents don’t include DOCTYPE lines, so this is moderately important rather than critical."
  },
  {
    "section": "HTML blocks",
    "example": 182,
    "weight": 0.5,
    "explanation": "CDATA-style HTML blocks are not extremely common in general prose, but they do appear in technical docs, XML/HTML examples, and scripts; getting them wrong can corrupt or escape raw content and mis-parse subsequent Markdown, so support is moderately important though not as critical as core features like headings or links."
  },
  {
    "section": "HTML blocks",
    "example": 183,
    "weight": 0.6,
    "explanation": "This tests how indentation distinguishes an HTML comment HTML-block (up to 3 spaces) from a 4-space indented code block — a common pattern when mixing HTML and Markdown. It's not as fundamental as headings but appears fairly often in docs/site content; getting it wrong can expose or hide content or display it as code, which is confusing for readers."
  },
  {
    "section": "HTML blocks",
    "example": 184,
    "weight": 0.8,
    "explanation": "Treating HTML blocks vs indented code blocks (up to three spaces allowed for an HTML block start; four spaces becomes a code block) is a common pattern — misrendering will often escape or hide intended raw HTML or unexpectedly execute it, changing layout and confusing readers, so this is an important correctness issue."
  },
  {
    "section": "HTML blocks",
    "example": 185,
    "weight": 0.7,
    "explanation": "Raw HTML blocks (e.g., <div>, <table>, <iframe>) are commonly used in real-world Markdown; getting their start/end rules wrong can lead to large portions of content being incorrectly parsed as Markdown or not rendered as intended, which is important though not as fundamental as headings/links."
  },
  {
    "section": "HTML blocks",
    "example": 186,
    "weight": 0.8,
    "explanation": "Mixing raw HTML blocks and Markdown is common (READMEs, docs, blogs); if a renderer fails to end a type‑6 HTML block at the blank line it will either swallow following Markdown as raw HTML or mis-parse the HTML, causing visible loss of formatting (e.g. emphasized text) and confusing readers."
  },
  {
    "section": "HTML blocks",
    "example": 187,
    "weight": 0.7,
    "explanation": "This example tests whether an inline-level tag on a line by itself is treated as part of the paragraph (type 7 cannot interrupt) rather than as a separate HTML block — a subtle but real parsing difference. Incorrect rendering can unexpectedly split or wrap paragraphs and alter link/HTML behavior in many real documents, so it is important though not as fundamental as headings or code blocks."
  },
  {
    "section": "HTML blocks",
    "example": 188,
    "weight": 0.7,
    "explanation": "Block-level HTML wrappers like <div> around Markdown content are fairly common (README files, blogs, docs), and the rule that type-6 HTML blocks end at a blank line so inner content is parsed as Markdown affects whether emphasis and paragraphs are recognized. Incorrect handling would often produce visibly wrong output (literal asterisks or missing paragraph structure), so this is important though not as foundational as headings or code blocks."
  },
  {
    "section": "HTML blocks",
    "example": 189,
    "weight": 0.7,
    "explanation": "Block-level HTML is fairly common in real-world Markdown (e.g., <div>, <table>), and getting whether content inside an HTML block is treated as raw HTML versus parsed Markdown wrong can visibly change rendering and confuse readers; it's not as fundamental as basic block types but is an important, frequently-encountered behavior to get right."
  },
  {
    "section": "HTML blocks",
    "example": 190,
    "weight": 1.0,
    "explanation": "Raw HTML blocks are commonly used (tables, divs, iframes, scripts) and are fundamental to preserving document structure; incorrect handling would frequently break layout and semantics and be highly confusing to readers."
  },
  {
    "section": "HTML blocks",
    "example": 191,
    "weight": 0.5,
    "explanation": "Handling of HTML blocks and blank-line termination is an important detail because many documents include raw HTML, and getting it wrong can produce very confusing output (raw tags or code blocks instead of intended HTML). However the specific blank-line/indentation interaction shown is a somewhat uncommon edge case, so it is moderately important rather than critical."
  },
  {
    "section": "Link reference definitions",
    "example": 192,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are widely used in READMEs, docs and other markdown content; failure to support them (or to handle their common variants) would frequently break links and confuse readers, so this is a foundational feature."
  },
  {
    "section": "Link reference definitions",
    "example": 193,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are a common, foundational Markdown feature used widely in READMEs and documentation; incorrect handling (including indentation/newline/title rules) will break or hide links and confuse readers, so failures are critical."
  },
  {
    "section": "Link reference definitions",
    "example": 194,
    "weight": 0.8,
    "explanation": "Reference-style links are common in documentation and READMEs; this example checks escaping and parentheses in labels/destinations/titles. Incorrect handling would break or hide links and confuse readers, so support is fairly important even if the specific escaping case is somewhat specialized."
  },
  {
    "section": "Link reference definitions",
    "example": 195,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are a basic, widely used Markdown feature in READMEs and documentation; failing to support them would break links or show raw markup, which is highly confusing and undermines document usefulness."
  },
  {
    "section": "Link reference definitions",
    "example": 196,
    "weight": 0.7,
    "explanation": "Reference-style link definitions are common in READMEs and docs, and incorrect parsing can produce visible stray text or broken links; the specific multiline-title syntax is less frequent but still useful and a parser bug would be noticeably disruptive to document rendering."
  },
  {
    "section": "Link reference definitions",
    "example": 197,
    "weight": 0.5,
    "explanation": "This is a moderately useful edge case: link reference definitions are common, but titles spanning multiple lines with an intervening blank line are rare. Incorrect handling can change visible text into a hidden definition or vice versa, which is confusing, but it is not a foundational Markdown feature used frequently in typical documents."
  },
  {
    "section": "Link reference definitions",
    "example": 198,
    "weight": 0.9,
    "explanation": "Reference-style link definitions are widely used in READMEs, docs and technical writing; incorrect handling (missing definitions, wrong precedence, case-sensitivity, multi-line titles, or scoping) will commonly produce broken or misleading links, so correct support is highly important."
  },
  {
    "section": "Link reference definitions",
    "example": 199,
    "weight": 0.7,
    "explanation": "Reference-style link definitions are fairly common in READMEs and documentation, and definitions placed inside lists or blockquotes should apply to the whole document; getting this wrong can break many links or cause visible literal text, which is confusing though not as fundamental as basic block/inline elements."
  },
  {
    "section": "Link reference definitions",
    "example": 200,
    "weight": 0.9,
    "explanation": "Link reference definitions are widely used in READMEs, docs, and generated Markdown; failure to support them (including empty destinations like <> ) would break many links or change layout and thus be highly disruptive, making this a near-foundational feature."
  },
  {
    "section": "Link reference definitions",
    "example": 201,
    "weight": 0.3,
    "explanation": "This is an edge-case about exact spacing/formatting of link reference definitions (angle-bracket destination immediately followed by parentheses) — uncommon in real documents. Getting it wrong could confuse a few cases, but it isn’t a foundational Markdown feature and is relatively niche."
  },
  {
    "section": "Link reference definitions",
    "example": 202,
    "weight": 0.8,
    "explanation": "Reference-style link definitions are common in READMEs and documentation, so incorrect handling would frequently affect real documents. Mishandling escapes/backslashes or percent-encoding in destinations and titles can produce broken links or incorrect titles, which is confusing and can break navigation."
  },
  {
    "section": "Link reference definitions",
    "example": 203,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are widely used in real-world Markdown (READMEs, docs, chat outputs) and integral to correct link rendering; failing to support them or rendering them incorrectly would frequently produce broken or visible raw definitions and confuse readers."
  },
  {
    "section": "Link reference definitions",
    "example": 204,
    "weight": 0.9,
    "explanation": "Reference-style link definitions are common in READMEs and technical docs; incorrect handling (wrong destination/title parsing, precedence, case-insensitivity, or placement rules) will break or mislink many links and noticeably confuse readers."
  },
  {
    "section": "Link reference definitions",
    "example": 205,
    "weight": 0.7,
    "explanation": "Link reference definitions are common in READMEs and documentation; the rule that definitions inside block containers are global is non‑obvious but important — getting it wrong can silently break or change many links and confuse readers, though it's not as fundamental as headings or basic emphasis."
  },
  {
    "section": "Link reference definitions",
    "example": 206,
    "weight": 0.75,
    "explanation": "Reference-style links are common in real-world docs, and correct handling of Unicode labels (case-insensitive matching) and non‑ASCII destinations (proper percent-encoding) is important for multilingual content; failures would break links and confuse readers even if this is not as foundational as headings or basic emphasis."
  },
  {
    "section": "Link reference definitions",
    "example": 207,
    "weight": 1.0,
    "explanation": "Link reference definitions are a foundational and widely used feature in real-world Markdown (READMEs, docs, chat, generated content); failing to support them correctly would break many links and significantly confuse readers."
  },
  {
    "section": "Link reference definitions",
    "example": 208,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are widely used in real-world Markdown (READMEs, docs, generated content); if a renderer fails to support them or handles them incorrectly many links will break or display as raw text, causing significant confusion and loss of functionality."
  },
  {
    "section": "Link reference definitions",
    "example": 209,
    "weight": 0.5,
    "explanation": "Link reference definitions are common and important, but this example tests a specific edge case (trailing non‑whitespace after the title invalidates the definition). Getting it wrong can be confusing (either losing visible text or unexpectedly defining a link), but it is not as foundational as headings/inline links, so the impact is moderate."
  },
  {
    "section": "Link reference definitions",
    "example": 210,
    "weight": 0.5,
    "explanation": "Reference-style link definitions are common and mis-parsing them can hide or alter visible text, but the specific multiline/title-after-newline nuance shown here is a somewhat uncommon edge case rather than a fundamental rendering feature."
  },
  {
    "section": "Link reference definitions",
    "example": 211,
    "weight": 0.7,
    "explanation": "Link reference definitions are common in READMEs and docs, and the interaction with indentation (4-space code blocks) can materially change whether a definition is active or shown as code; incorrect rendering would often confuse readers by turning visible code into a hidden link or vice versa."
  },
  {
    "section": "Link reference definitions",
    "example": 212,
    "weight": 0.5,
    "explanation": "This is an occasional but important edge case: link reference definitions are common, and mis-parsing definitions that occur inside code blocks could hide code or change link behavior, but the specific pattern (definitions inside fenced/indented code) is not extremely frequent in everyday documents."
  },
  {
    "section": "Link reference definitions",
    "example": 213,
    "weight": 0.9,
    "explanation": "Reference-style link definitions are common in READMEs, docs, and generated markdown; lack of support causes visible raw definitions or broken/missing links and is therefore highly disruptive to readability and navigation."
  },
  {
    "section": "Link reference definitions",
    "example": 214,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are widely used in READMEs, documentation, and markdown authored by humans and tools; incorrect or missing support will break links, alter document structure, and be highly confusing, so this is a foundational feature."
  },
  {
    "section": "Link reference definitions",
    "example": 215,
    "weight": 1.0,
    "explanation": "Link reference definitions are a fundamental, widely used Markdown feature in READMEs and documentation; incorrect handling would break or hide links and change visible content, so correct rendering is critical."
  },
  {
    "section": "Link reference definitions",
    "example": 216,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are widely used in READMEs, docs and chat output and are fundamental to correct link rendering; mis-handling them (or treating them as visible content or as structural elements) will break links and can change surrounding block parsing (e.g. headings), causing confusing, incorrect output."
  },
  {
    "section": "Link reference definitions",
    "example": 217,
    "weight": 0.9,
    "explanation": "Reference-style link definitions are widely used in READMEs, docs and long-form markdown to keep text clean; incorrect handling (missing, mis-parsed, or scoping errors) will frequently break links, hide content, or produce confusing output, so correct support is highly important though slightly less universal than basic block-level elements."
  },
  {
    "section": "Link reference definitions",
    "example": 218,
    "weight": 1.0,
    "explanation": "Reference-style link definitions are commonly used in READMEs, documentation, and long-form Markdown to keep markup readable; if a renderer fails to recognize them or scopes them incorrectly it will break links or expose raw definition text, which is highly confusing and damaging to documents."
  },
  {
    "section": "Paragraphs",
    "example": 219,
    "weight": 1.0,
    "explanation": "Paragraph parsing is foundational and appears in virtually all Markdown documents; getting blank-line separation, leading-space allowances, and trailing-space trimming wrong would fundamentally break document structure and confuse readers."
  },
  {
    "section": "Paragraphs",
    "example": 220,
    "weight": 1.0,
    "explanation": "Paragraph handling is a foundational feature used everywhere in Markdown (separating blocks, treating blank lines and indentation), so incorrect rendering would regularly break large numbers of documents and confuse readers."
  },
  {
    "section": "Paragraphs",
    "example": 221,
    "weight": 1.0,
    "explanation": "Paragraph parsing is foundational to nearly all Markdown content—incorrect handling of blank lines, indentation limits, or trailing-space stripping would break basic text flow and be highly confusing for readers."
  },
  {
    "section": "Paragraphs",
    "example": 222,
    "weight": 1.0,
    "explanation": "Paragraph parsing is a fundamental Markdown behavior used everywhere; rules about line joining, indentation limits, blank-line separation, and trimming determine basic layout and line breaks, so incorrect handling would break most documents and be highly confusing."
  },
  {
    "section": "Paragraphs",
    "example": 223,
    "weight": 1.0,
    "explanation": "Paragraph handling is foundational and ubiquitous: most Markdown documents rely on correct joining of non-blank lines, trimming of leading/trailing spaces, and the up-to-3-space indentation rule (4 spaces = code). Incorrect rendering would break basic text flow in virtually all documents and be highly confusing."
  },
  {
    "section": "Paragraphs",
    "example": 224,
    "weight": 1.0,
    "explanation": "Paragraphs are a foundational feature used in almost every Markdown document; incorrect handling of multi-line paragraphs, leading indentation (3 vs 4 spaces), blank lines, or trailing spaces would frequently alter structure and meaning and thus be highly disruptive."
  },
  {
    "section": "Paragraphs",
    "example": 225,
    "weight": 1.0,
    "explanation": "Paragraph vs indented code-block handling is a foundational Markdown behavior used frequently in real documents; getting the 3-vs-4-space threshold or paragraph parsing wrong will commonly misclassify content and be confusing to readers."
  },
  {
    "section": "Paragraphs",
    "example": 226,
    "weight": 0.8,
    "explanation": "Two-space hard line breaks are a common Markdown idiom and the rule about stripping trailing spaces on the final line prevents spurious breaks; getting this wrong would frequently produce missing or extra <br/>s in READMEs, docs, and prose and therefore is noticeably harmful though not as critical as headings or code blocks."
  },
  {
    "section": "Blank lines",
    "example": 227,
    "weight": 1.0,
    "explanation": "Blank-line handling between block elements is fundamental and extremely common; getting this wrong changes block boundaries (extra empty paragraphs or merged blocks) and would frequently and obviously break real-world documents."
  },
  {
    "section": "Block quotes",
    "example": 228,
    "weight": 0.7,
    "explanation": "Block quotes are a common, foundational block-level feature (used in READMEs, docs and chat) and getting their nesting, lazy continuation, and indentation rules wrong can change document structure and confuse readers, so support is important though not as critical as core links/headings."
  },
  {
    "section": "Block quotes",
    "example": 229,
    "weight": 0.7,
    "explanation": "Block quotes are a common structural element in real-world Markdown (emails, READMEs, chat), and subtle rules like optional spaces, laziness (continuation lines) and indentation affect paragraph grouping and nesting; incorrect handling would frequently change document structure and confuse readers, though it's not as fundamental as headings or links."
  },
  {
    "section": "Block quotes",
    "example": 230,
    "weight": 0.7,
    "explanation": "Block quotes are a common structural element (used in READMEs, docs, chat/email quotes) and getting their delimiting, laziness, and nesting rules wrong can change document structure (paragraph vs list vs code block) and confuse readers, but they are slightly less fundamental than headings/links/code blocks."
  },
  {
    "section": "Block quotes",
    "example": 231,
    "weight": 0.7,
    "explanation": "Block quotes are a common construct in real-world docs, chat and READMEs; the Laziness, indentation and consecutiveness rules affect whether lines remain inside a quote or become lists/code/paragraphs, so incorrect handling would frequently produce confusing or semantically different output even though it's not as foundational as headings or links."
  },
  {
    "section": "Block quotes",
    "example": 232,
    "weight": 0.7,
    "explanation": "Block quotes are a common, foundational feature and the 'laziness' rule (omitting '>' on continuation lines) is a subtle but real-world pattern; getting it wrong would often produce misleading structure (lines mistakenly outside the quote), so it is important though somewhat more specific than the very basics."
  },
  {
    "section": "Block quotes",
    "example": 233,
    "weight": 0.7,
    "explanation": "Block quotes are commonly used in prose, READMEs, emails and chat; the laziness rules (omitting > on continuation lines), indentation allowances, and consecutiveness affect normal authoring and can cause paragraphs to be merged or split incorrectly if implemented wrong, so correct handling is important though not as critical as core elements like headings or links."
  },
  {
    "section": "Block quotes",
    "example": 234,
    "weight": 0.8,
    "explanation": "Block quotes are a common structural element and the laziness rule affects how subsequent blocks (here a thematic break) are attached; getting this wrong can change document structure (placing the HR inside the quote) and confuse readers, so this is an important behavior to render correctly."
  },
  {
    "section": "Block quotes",
    "example": 235,
    "weight": 0.7,
    "explanation": "Block quotes are common in real-world markdown (emails, READMEs, docs) and getting their boundaries wrong — especially lazy continuations — can change whether content (like lists) appears inside or outside the quote, producing misleading structure; it's important but somewhat more specialized than the absolute basics."
  },
  {
    "section": "Block quotes",
    "example": 236,
    "weight": 0.7,
    "explanation": "Block quotes are common and nesting code blocks inside them appears regularly in docs; getting the required indentation wrong will often move or drop code blocks out of the quote and produce confusing output. This is an important formatting detail (affecting readability and structure) though slightly less foundational than headings/links."
  },
  {
    "section": "Block quotes",
    "example": 237,
    "weight": 0.7,
    "explanation": "Block quotes are a common Markdown feature (used in docs, email/chat quoting, READMEs), and incorrect handling — especially of laziness/continuation lines and interactions with lists or code blocks — can materially change document structure. While not as foundational as headings or links, block-quote errors are important and frequently noticeable."
  },
  {
    "section": "Block quotes",
    "example": 238,
    "weight": 0.7,
    "explanation": "Block quotes are a common Markdown feature (used in docs, READMEs and chat) and the laziness rule affects whether lines are included in a quote or terminate it; incorrect handling can change structure and meaning, so this is an important (but not core-level) feature to support correctly."
  },
  {
    "section": "Block quotes",
    "example": 239,
    "weight": 0.5,
    "explanation": "Block quotes are a common feature, but an empty block quote (a lone \">\" producing an empty <blockquote>) is relatively rare; incorrect handling would rarely confuse most readers but could break intended structure or spacing, so it's of moderate practical importance."
  },
  {
    "section": "Block quotes",
    "example": 240,
    "weight": 0.7,
    "explanation": "Block quotes are a common Markdown feature (used in READMEs, docs and chat quoting) and subtle rules like laziness and empty blockquotes affect how content is grouped; incorrect handling can produce confusing merges or missing quote blocks, so support is important though not as critical as core elements like headings or code blocks."
  },
  {
    "section": "Block quotes",
    "example": 241,
    "weight": 0.7,
    "explanation": "Block quotes are a common block-level feature in docs, READMEs, chats and email quoting; mishandling empty markers, laziness/continuation lines or blank-line separation would frequently change document structure and confuse readers. It's important but slightly less fundamental than headings/links, so a 0.7 rating is appropriate."
  },
  {
    "section": "Block quotes",
    "example": 242,
    "weight": 0.9,
    "explanation": "Block quotes are a common, foundational Markdown feature used in docs, READMEs, and chat; incorrect handling of markers, indentation, laziness, or blank-line separation would frequently break intended structure and confuse readers."
  },
  {
    "section": "Block quotes",
    "example": 243,
    "weight": 0.7,
    "explanation": "Block quotes are a common Markdown feature (used in READMEs, docs and chat transcripts) and the laziness/spacing rules are subtle; incorrect handling can merge or split quoted content or misplace paragraphs, leading to confusing output."
  },
  {
    "section": "Block quotes",
    "example": 244,
    "weight": 1.0,
    "explanation": "Block quotes are a fundamental Markdown feature used frequently in documentation, READMEs, email-style content and chat; incorrect handling of markers, optional spaces, or lazy continuation lines would regularly change document structure (merge/split paragraphs, break nesting) and be highly confusing to readers."
  },
  {
    "section": "Block quotes",
    "example": 245,
    "weight": 1.0,
    "explanation": "Block quotes are a foundational Markdown feature used frequently in documentation, READMEs, chat logs and AI output; incorrect handling (including laziness rules) would noticeably alter document structure and meaning and thus be highly misleading to readers."
  },
  {
    "section": "Block quotes",
    "example": 246,
    "weight": 0.7,
    "explanation": "Block quotes (and their interaction with other block elements like horizontal rules) are common in real-world docs and chat content; getting them wrong (merging/splitting blockquotes incorrectly) would noticeably misrepresent document structure and confuse readers, so this is important but not absolutely critical."
  },
  {
    "section": "Block quotes",
    "example": 247,
    "weight": 0.9,
    "explanation": "Block quotes are a common, foundational Markdown feature and the laziness rule (omitting > on continuation lines) appears frequently in real-world writing; getting this wrong will often split content incorrectly or place text outside the quote, which is confusing and changes meaning."
  },
  {
    "section": "Block quotes",
    "example": 248,
    "weight": 0.7,
    "explanation": "Block quotes are a common formatting feature (quotes in docs, READMEs, chat) and incorrect handling of markers, laziness or blank-line rules can merge or split content unexpectedly and confuse readers; important but not as critical as links or headings."
  },
  {
    "section": "Block quotes",
    "example": 249,
    "weight": 0.7,
    "explanation": "Block quotes are commonly used and the laziness rule is a frequent subtlety; rendering this incorrectly can cause following text to be mistakenly included in or excluded from the quote, which would be confusing, so it’s important though somewhat more specific than core features like headings or links."
  },
  {
    "section": "Block quotes",
    "example": 250,
    "weight": 0.8,
    "explanation": "Block quotes (including nested quotes and lazy continuation lines) occur frequently in email, forums, READMEs and chat logs; getting laziness or omitted leading >s wrong will often break the intended quoting structure and confuse readers, so correct handling is important."
  },
  {
    "section": "Block quotes",
    "example": 251,
    "weight": 0.7,
    "explanation": "Block quotes (including nesting and lazy continuation) are commonly used (emails, forums, READMEs); getting them wrong can change document structure and confuse readers, though this is not as fundamental as headings/links."
  },
  {
    "section": "Block quotes",
    "example": 252,
    "weight": 0.8,
    "explanation": "Block quotes are a common Markdown feature in READMEs, docs and chat; the laziness and indentation rules (omitting > on continuation lines, up-to-three-space indentation, the five-space rule for code) affect how paragraphs, lists and code inside quotes are parsed. Wrong handling would frequently produce misleading structure (merged/split paragraphs, lost code blocks or lists), so correct support is important though not as fundamental as headings/links."
  },
  {
    "section": "List items",
    "example": 253,
    "weight": 1.0,
    "explanation": "This tests fundamental block-level features — paragraphs, indented (4‑space) code blocks, and blockquotes — which are common in real-world Markdown (including many READMEs and legacy documents); misrendering them would be highly confusing and break expected content structure."
  },
  {
    "section": "List items",
    "example": 254,
    "weight": 1.0,
    "explanation": "List items with mixed contents (multi-line paragraphs, indented code blocks, nested blockquotes) are extremely common in READMEs and docs; getting their indentation and containment wrong breaks structure and code examples and is therefore critical to render correctly."
  },
  {
    "section": "List items",
    "example": 255,
    "weight": 1.0,
    "explanation": "List continuation vs a separate paragraph is a common and foundational parsing rule; getting this wrong changes document structure and meaning in many READMEs, docs and chat output, so incorrect rendering would be highly disruptive."
  },
  {
    "section": "List items",
    "example": 256,
    "weight": 1.0,
    "explanation": "Multi-paragraph list items (a blank line then a continuation indented as shown) are common in READMEs and documentation; misrendering causes paragraphs to be split out of the list or lists to be broken, which materially changes meaning and layout, so correct handling is critical."
  },
  {
    "section": "List items",
    "example": 257,
    "weight": 0.7,
    "explanation": "The interaction between list-item indentation and indented code blocks appears fairly often in READMEs and docs; if rendered incorrectly it can change document structure (code becoming part of a list or vice versa) and be confusing, so it is an important but not absolutely foundational feature."
  },
  {
    "section": "List items",
    "example": 258,
    "weight": 1.0,
    "explanation": "List-item indentation and continuation rules are foundational and common in real-world Markdown; getting them wrong changes document structure (merging or splitting list items, breaking nested lists or code blocks) and is therefore critical to render correctly."
  },
  {
    "section": "List items",
    "example": 259,
    "weight": 0.7,
    "explanation": "This tests nuanced indentation rules for lists inside nested blockquotes; lists and blockquotes are common and getting this wrong can change document structure and meaning, but it's not as universally foundational as headings or basic emphasis."
  },
  {
    "section": "List items",
    "example": 260,
    "weight": 0.7,
    "explanation": "This tests subtle interaction of lists and nested blockquotes—patterns that appear often enough in real documents and can substantially change structure (list vs paragraph) if rendered incorrectly, so it’s important though more specialized than the most basic Markdown features."
  },
  {
    "section": "List items",
    "example": 261,
    "weight": 1.0,
    "explanation": "Correctly requiring a space after list markers is a fundamental list-parsing rule; getting it wrong would frequently turn ordinary text (e.g. -one, 2.two, version-like strings or hyphenated words) into unintended list items and significantly confuse readers."
  },
  {
    "section": "List items",
    "example": 262,
    "weight": 1.0,
    "explanation": "This tests multi-paragraph list items (blank-line continuation inside a list), a common pattern in real-world docs and READMEs; getting it wrong can split lists or misplace paragraphs, producing confusing or incorrect structure, so it is a fundamental feature."
  },
  {
    "section": "List items",
    "example": 263,
    "weight": 1.0,
    "explanation": "Lists containing fenced code blocks, subsequent paragraphs, and nested blockquotes are very common in READMEs, docs, and chat output; misrendering these list-item containment rules would break document structure and be highly confusing, so correct handling is critical."
  },
  {
    "section": "List items",
    "example": 264,
    "weight": 0.9,
    "explanation": "Code blocks inside list items (including preserving blank lines in indented code) are a common, foundational pattern in READMEs and docs; getting this wrong would frequently confuse readers or hide important code examples, even though fenced code reduces some impact."
  },
  {
    "section": "List items",
    "example": 265,
    "weight": 0.3,
    "explanation": "This tests large ordered-list start numbers (up to 9 digits), which are an uncommon edge case; most documents use small start values, so incorrect handling would be a minor, non-critical rendering bug."
  },
  {
    "section": "List items",
    "example": 266,
    "weight": 0.1,
    "explanation": "This is an obscure edge case (10+ digit ordered-list markers) that rarely occurs in real documents; misrendering would produce an unexpected list but would rarely affect typical content, so its practical impact is minimal."
  },
  {
    "section": "List items",
    "example": 267,
    "weight": 0.6,
    "explanation": "Allowing an ordered list to start at 0 is fairly common in technical/programming docs and affects the rendered numbering and the ol start attribute; misrendering (forcing start at 1 or omitting the start attribute) can confuse readers and break semantics, but it is not as fundamental as core features like headings or links."
  },
  {
    "section": "List items",
    "example": 268,
    "weight": 0.5,
    "explanation": "This tests that ordered-list markers with leading zeros are parsed as numeric starts (here 003 → start=3). It’s not extremely common, but appears occasionally (auto‑generated or padded lists) and incorrect handling can change rendered numbering and confuse readers, so it’s moderately important."
  },
  {
    "section": "List items",
    "example": 269,
    "weight": 0.5,
    "explanation": "This is a list-marker detection edge case (no space after the '-' so it should be a paragraph). It’s not uncommon to see strings like \"-1.\" in text, and mis-parsing them as a list is confusing, but it’s a relatively specific parsing nuance rather than a foundational feature."
  },
  {
    "section": "List items",
    "example": 270,
    "weight": 1.0,
    "explanation": "Lists containing indented code blocks are common in READMEs and docs; getting this wrong breaks document structure and code formatting in a highly visible and confusing way, so correct support is critical."
  },
  {
    "section": "List items",
    "example": 271,
    "weight": 0.9,
    "explanation": "Correctly handling indented code blocks inside list items (including ordered lists with multi-digit start numbers) is a core renderer feature used frequently in READMEs and docs; getting it wrong can break list structure or turn code into regular text, significantly changing meaning."
  },
  {
    "section": "List items",
    "example": 272,
    "weight": 1.0,
    "explanation": "This tests indented (4‑space) code blocks and their separation from paragraphs — a very common, foundational Markdown feature; incorrect handling would frequently break code display and paragraph structure and be highly confusing to readers."
  },
  {
    "section": "List items",
    "example": 273,
    "weight": 0.7,
    "explanation": "Lists containing code blocks (especially in READMEs and technical docs) are common; getting the indentation rules for list items that start with indented code wrong will frequently break structure and confuse readers by losing list semantics or misclassifying code vs. paragraphs. It's not as fundamental as headings but is an important, frequently encountered behavior."
  },
  {
    "section": "List items",
    "example": 274,
    "weight": 0.7,
    "explanation": "Lists containing indented code blocks and following paragraphs appear reasonably often in READMEs, tutorials and docs; while fenced code has reduced reliance on indented code, mis-rendering these rules would break list structure and be confusing to readers. This is an important (but somewhat specialized) parsing behavior rather than a rare spec trivia."
  },
  {
    "section": "List items",
    "example": 275,
    "weight": 0.8,
    "explanation": "This is about how small (≤3) leading indents are treated (not as code blocks or list markers) — a renderer getting this wrong can turn ordinary paragraphs into code/list items or merge/split paragraphs incorrectly. This pattern appears reasonably often (accidental indentation, formatted text) and affects basic paragraph/code-block semantics, so it’s important."
  },
  {
    "section": "List items",
    "example": 276,
    "weight": 0.9,
    "explanation": "List-item continuation/indentation rules are foundational and common; incorrect handling will often merge or split paragraphs and sublists incorrectly, substantially changing document structure and confusing readers."
  },
  {
    "section": "List items",
    "example": 277,
    "weight": 1.0,
    "explanation": "Paragraph continuation inside list items is a fundamental, frequently used pattern in READMEs, docs and chat; getting it wrong will split or reorder content (separating a paragraph from its list item), which is confusing and breaks document structure."
  },
  {
    "section": "List items",
    "example": 278,
    "weight": 1.0,
    "explanation": "Lists containing paragraphs and code blocks (both fenced and indented) are extremely common (READMEs, docs, chat), and incorrect handling breaks structure and code formatting, causing significant confusion."
  },
  {
    "section": "List items",
    "example": 279,
    "weight": 0.7,
    "explanation": "This is an important but somewhat specific list behavior: many authors put the list marker on its own line (or with trailing spaces) and indent the content on the following line; if a renderer gets this wrong the list can be split or the content rendered outside the list, which is confusing for readers."
  },
  {
    "section": "List items",
    "example": 280,
    "weight": 0.6,
    "explanation": "This tests list-item boundary behavior (an empty list item followed by a separate paragraph). List parsing is a foundational feature so mistakes can noticeably break document structure, but empty list items like this are relatively uncommon, so the impact is moderate–important."
  },
  {
    "section": "List items",
    "example": 281,
    "weight": 0.6,
    "explanation": "This checks handling of empty list items — lists are a foundational feature and appear often, but explicitly empty list elements are a relatively uncommon edge case; incorrect rendering could drop or merge items and cause loss/misrepresentation of content, though it is not as critical as basic list/formatting support."
  },
  {
    "section": "List items",
    "example": 282,
    "weight": 0.7,
    "explanation": "Lists are a foundational Markdown feature and handling empty list items correctly affects structure and intent (e.g., deliberate blank bullets or spacing); while empty items are less common than regular list content, incorrect rendering would be noticeable and potentially misleading."
  },
  {
    "section": "List items",
    "example": 283,
    "weight": 0.7,
    "explanation": "List parsing is a core Markdown feature and empty list items, while not extremely common, do appear (placeholders, formatting mistakes) and must be preserved; incorrect handling would change structure/numbering and confuse readers, so this is important but a somewhat specific list-edge case."
  },
  {
    "section": "List items",
    "example": 284,
    "weight": 0.9,
    "explanation": "List items are a foundational Markdown feature and lists are very common; supporting empty list items is a basic part of correct list parsing. Incorrect handling would break document structure and can be confusing or surprising to users and tooling that expects standard list behavior."
  },
  {
    "section": "List items",
    "example": 285,
    "weight": 0.9,
    "explanation": "This verifies the fundamental rule that list markers must be followed by a space/tab — lone `*` or `1.` lines should remain plain text. Misinterpreting this would frequently turn ordinary text into unintended lists or drop content, so correct handling is important."
  },
  {
    "section": "List items",
    "example": 286,
    "weight": 1.0,
    "explanation": "Correct handling of list-item indentation and mixed contents (paragraphs, indented code blocks, blockquotes) is a foundational, frequently used Markdown pattern (READMEs, docs, comments); incorrect rendering would widely confuse document structure and meaning."
  },
  {
    "section": "List items",
    "example": 287,
    "weight": 1.0,
    "explanation": "List items with multi-paragraph content, indented code blocks, and blockquotes are very common in documentation and READMEs; getting the indentation rules wrong changes document structure and meaning, so correct rendering is critical."
  },
  {
    "section": "List items",
    "example": 288,
    "weight": 1.0,
    "explanation": "List-item indentation, continuation lines, and nested blocks (paragraphs, indented code, blockquotes) are very common in real-world Markdown; getting them wrong changes document structure and meaning and is thus a critical, foundational feature."
  },
  {
    "section": "List items",
    "example": 289,
    "weight": 1.0,
    "explanation": "This tests indented code blocks (which must not be parsed as lists or blockquotes and must preserve/escape their contents); code blocks are a foundational Markdown feature and misrendering them would frequently break intent and be highly confusing."
  },
  {
    "section": "List items",
    "example": 290,
    "weight": 1.0,
    "explanation": "Lists containing multi-line paragraphs, indented code blocks, and nested blocks are very common in real-world Markdown; incorrect handling of list-marker indentation fundamentally changes document structure and is highly disruptive to readers."
  },
  {
    "section": "List items",
    "example": 291,
    "weight": 1.0,
    "explanation": "List continuation/indentation is a fundamental Markdown feature used frequently (multi-line list items, nested lists, README docs). Incorrect handling of the required indentation (how many spaces after the marker determine continuation) will commonly break list structure or merge/split paragraphs unexpectedly, causing significant confusion."
  },
  {
    "section": "List items",
    "example": 292,
    "weight": 0.7,
    "explanation": "Nested lists and blockquotes with lazy continuation lines are fairly common in real-world Markdown (especially in READMEs and forums); misrendering would often misassociate text between quote/list levels and be confusing, so this is an important but not strictly core feature."
  },
  {
    "section": "List items",
    "example": 293,
    "weight": 0.7,
    "explanation": "This tests nesting and continuation rules (blockquote → ordered list → inner blockquote), which occur reasonably often in quoted emails/threads and docs; incorrect rendering would misassociate content and be confusing, so it is an important (though not foundational) case."
  },
  {
    "section": "List items",
    "example": 294,
    "weight": 1.0,
    "explanation": "Nested lists are a very common Markdown pattern (READMEs, docs, notes); failing to render their indentation and nesting correctly would break document structure and be highly confusing to readers, so this is a critical feature."
  },
  {
    "section": "List items",
    "example": 295,
    "weight": 1.0,
    "explanation": "This tests a fundamental list rule (markers may be indented up to three spaces) — lists are ubiquitous in real-world Markdown and incorrect handling would frequently break structure and highly confuse readers."
  },
  {
    "section": "List items",
    "example": 296,
    "weight": 1.0,
    "explanation": "Nested lists and ordered-list start numbers are common in real-world docs (READMEs, guides); failing to nest the `- bar` under `10) foo` or to honor the start number would break structure and meaning, so this is a critical feature."
  },
  {
    "section": "List items",
    "example": 297,
    "weight": 1.0,
    "explanation": "List nesting and indentation are foundational and very common; getting the rule for multi-digit ordered markers and subsequent indentation wrong will frequently change document structure (nested vs separate lists) and produce confusing/misleading output."
  },
  {
    "section": "List items",
    "example": 298,
    "weight": 0.9,
    "explanation": "Nested lists are a common and foundational Markdown pattern; failing to recognize a list item that contains another list (as in \"- - foo\") would frequently alter document structure and be confusing or misleading to readers."
  },
  {
    "section": "List items",
    "example": 299,
    "weight": 0.3,
    "explanation": "This is a fairly niche/edge-case way to write nested lists (multiple list markers on one line). Most real-world documents use separate lines for nested lists, so incorrect rendering would be confusing but not commonly encountered or critical."
  },
  {
    "section": "List items",
    "example": 300,
    "weight": 0.9,
    "explanation": "This tests how block content (headings and thematic breaks) continues inside list items — a foundational list/heading interaction that appears commonly in READMEs and docs; getting it wrong alters document structure and is therefore highly impactful."
  },
  {
    "section": "Lists",
    "example": 301,
    "weight": 0.3,
    "explanation": "Mixing bullet markers mid-list is relatively uncommon, so this is a niche parsing detail; incorrect rendering (merging vs. splitting lists) can change the HTML structure and semantics but usually has minor visual impact. It's not foundational Markdown behavior, though it can matter for precise document structure or styling."
  },
  {
    "section": "Lists",
    "example": 302,
    "weight": 0.7,
    "explanation": "Mixing ordered-list delimiters (e.g., 1. / 3)) is a fairly common real-world pattern and affects basic list structure — a renderer that joins or misnumbers such items will produce confusing output. This is important behavior (affects list grouping/numbering) but slightly less fundamental than core elements like headings or code blocks."
  },
  {
    "section": "Lists",
    "example": 303,
    "weight": 1.0,
    "explanation": "Allowing a list to interrupt a paragraph (no blank line) is a common, foundational list behavior; failing to support it would frequently break real-world documents and produce misleading/incorrect structure and rendering."
  },
  {
    "section": "Lists",
    "example": 304,
    "weight": 0.7,
    "explanation": "Hard-wrapped prose that begins a line with a numeral (e.g., counts, measurements) is common; if a renderer incorrectly treats numbers like \"14.\" as list markers it will split and reformat ordinary paragraphs in confusing ways. This is not core like headings but is an important, practical behavior to avoid spurious lists."
  },
  {
    "section": "Lists",
    "example": 305,
    "weight": 1.0,
    "explanation": "Very common: authors frequently start lists immediately after a paragraph (especially ordered lists beginning with “1.”) without a blank line. Misrecognizing list interruption (or mishandling the special rule about lists starting with 1) breaks core list structure and meaning in many READMEs, docs, and chat outputs, so incorrect rendering would be highly confusing."
  },
  {
    "section": "Lists",
    "example": 306,
    "weight": 1.0,
    "explanation": "List parsing (including handling of blank lines and loose vs. tight lists) is a fundamental Markdown feature used ubiquitously; incorrect handling changes paragraph wrapping and layout of list items and would frequently confuse readers and break formatting."
  },
  {
    "section": "Lists",
    "example": 307,
    "weight": 0.7,
    "explanation": "Nested lists and paragraph breaks inside list items are common in real-world docs; getting the blank-line/indentation rules wrong can noticeably change structure (turning a paragraph into a code block or a separate list), so correctness is important though not as foundational as headings or links."
  },
  {
    "section": "Lists",
    "example": 308,
    "weight": 0.3,
    "explanation": "Using an HTML comment to force separation of consecutive lists is a niche trick: useful in some authoring scenarios but uncommon in everyday documents. Incorrect handling would usually only cause merged lists (a minor rendering issue) rather than severe confusion."
  },
  {
    "section": "Lists",
    "example": 309,
    "weight": 0.5,
    "explanation": "This example checks list termination and the use of a blank HTML comment to prevent an indented code block from becoming part of the preceding list — a useful but somewhat niche pattern. While general list/code handling is common and mis-rendering can be confusing, the specific HTML-comment-as-separator is not frequently used in everyday documents."
  },
  {
    "section": "Lists",
    "example": 310,
    "weight": 0.9,
    "explanation": "List parsing and indentation rules are foundational and lists are extremely common; inconsistent indentation (from editing, copy/paste, or email clients) appears regularly. If a renderer misinterprets these indentation rules it can produce unintended nesting or flattening of list items, which is confusing and alters document meaning."
  },
  {
    "section": "Lists",
    "example": 311,
    "weight": 0.9,
    "explanation": "Lists are a foundational Markdown feature and authors often use inconsistent indentation and blank lines; getting this wrong can produce unintended nesting or code blocks and significantly alter document structure, so correct handling is important."
  },
  {
    "section": "Lists",
    "example": 312,
    "weight": 0.7,
    "explanation": "Lists and nested lists are very common in real-world Markdown; incorrect handling of the indentation/maximum‑3‑spaces rule will produce wrong nesting or literal markers (surprising to readers) even though it’s a somewhat specific list‑indentation edge case rather than a core inline feature."
  },
  {
    "section": "Lists",
    "example": 313,
    "weight": 0.7,
    "explanation": "This example tests list indentation vs. code-block behavior (4+ spaces turning a line into a code block rather than a nested list). Misrendering would commonly affect README/docs and notes where lists and code blocks are mixed and would be confusing to readers, so it is an important (though slightly specialized) list-handling rule."
  },
  {
    "section": "Lists",
    "example": 314,
    "weight": 1.0,
    "explanation": "This tests core list behavior (blank lines between items producing a single loose list with paragraphs); lists are extremely common and incorrect rendering would change document structure and meaning, so support is critical."
  },
  {
    "section": "Lists",
    "example": 315,
    "weight": 0.3,
    "explanation": "Empty list items and the tight/loose distinctions are relatively uncommon in real-world Markdown; most documents use non-empty items. Getting this wrong might be mildly confusing (missing blank <li> or different paragraph wrapping) but is not critical compared with core list, heading, or link behavior."
  },
  {
    "section": "Lists",
    "example": 316,
    "weight": 1.0,
    "explanation": "This tests multi-paragraph list-item continuation (an indented paragraph after a blank line belonging to the previous list item). Multi-paragraph lists are common and foundational; incorrect rendering would break document structure and confuse readers."
  },
  {
    "section": "Lists",
    "example": 317,
    "weight": 0.7,
    "explanation": "Reference link definitions placed between list items (often indented) appear in real-world docs and READMEs; getting this wrong can break or split lists or show stray block content, so it is an important (though not absolutely core) parsing behavior to support correctly."
  },
  {
    "section": "Lists",
    "example": 318,
    "weight": 1.0,
    "explanation": "Fenced code blocks inside list items are very common in READMEs, docs and tutorials; getting this wrong breaks basic document structure and confuses readers, so correct handling is essential."
  },
  {
    "section": "Lists",
    "example": 319,
    "weight": 0.8,
    "explanation": "This example tests nested lists and blank-line-separated paragraphs inside a nested list item — a pattern that appears regularly in READMEs and docs. Incorrect rendering would change the document structure and meaning (e.g., turning nested content into separate items or code), so it's an important behavior to get right."
  },
  {
    "section": "Lists",
    "example": 320,
    "weight": 0.7,
    "explanation": "This tests nested block-level content (a blockquote) inside a list item with specific indentation/blank-line behavior. Nested lists/blocks are fairly common in real documents and misrendering would confuse document structure, so it is important though not as fundamental as basic headings or inline formatting."
  },
  {
    "section": "Lists",
    "example": 321,
    "weight": 0.9,
    "explanation": "This example tests nested block elements (blockquote and fenced code) inside a list item — a common pattern in docs and READMEs. Incorrect rendering would break document structure and be confusing, so proper support is highly important."
  },
  {
    "section": "Lists",
    "example": 322,
    "weight": 1.0,
    "explanation": "Bullet lists are a foundational and extremely common Markdown construct (READMEs, docs, chat, AI output); failing to render a simple list correctly would be highly confusing and break a large amount of real-world content."
  },
  {
    "section": "Lists",
    "example": 323,
    "weight": 1.0,
    "explanation": "This example tests nested lists (a sublist indented under a parent list item), a very common and foundational Markdown pattern in docs, READMEs, and chat. Incorrect handling would substantially alter document structure and meaning, so correct support is critical."
  },
  {
    "section": "Lists",
    "example": 324,
    "weight": 1.0,
    "explanation": "Lists containing fenced code blocks (and subsequent paragraphs) are very common in technical docs, READMEs and tutorials; incorrect handling breaks document structure and meaning (code pulled out of list or paragraphs misplaced), so correct parsing is critical."
  },
  {
    "section": "Lists",
    "example": 325,
    "weight": 1.0,
    "explanation": "This example tests nested lists and multi-paragraph (loose) list items — a very common pattern in READMEs, docs, and notes. Incorrect handling would substantially alter document structure and meaning, so correct rendering is critical."
  },
  {
    "section": "Lists",
    "example": 326,
    "weight": 1.0,
    "explanation": "Nested lists and the rules for list interruption/indentation are extremely common in real-world markdown (READMEs, docs, chat), and misrendering them changes document structure and meaning — a fundamental list-parsing feature."
  },
  {
    "section": "Inlines",
    "example": 327,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental Markdown feature used widely in docs and READMEs; mishandling backtick delimiting (leaving stray backticks or absorbing surrounding text) would frequently confuse readers and break common usage."
  },
  {
    "section": "Code spans",
    "example": 328,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational, frequently used feature in READMEs, docs, and chat; incorrect handling of backtick delimiters, space trimming, or precedence with other inlines causes widespread, confusing rendering errors and broken meaning."
  },
  {
    "section": "Code spans",
    "example": 329,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational, frequently used feature; incorrect handling of backtick delimiters, whitespace-stripping rules, or precedence with emphasis/links would commonly break real-world documentation and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 330,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used widely in documentation, READMEs, and chats; incorrect handling of backtick delimiters, space-stripping rules, or precedence with other inlines would frequently break meaning and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 331,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used frequently in docs, READMEs, and chat; incorrect handling of backtick delimiters, space-stripping, or precedence against other inlines would regularly confuse readers and break common content."
  },
  {
    "section": "Code spans",
    "example": 332,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental Markdown feature used widely in docs and chat; incorrect handling of delimiters, space-stripping, backtick nesting or precedence would frequently break meaning and confuse readers, so correct support is critical."
  },
  {
    "section": "Code spans",
    "example": 333,
    "weight": 0.5,
    "explanation": "Code spans are a fundamental feature, but this example tests a relatively subtle rule (only U+0020 spaces are stripped when both leading and trailing). That behavior is uncommon in everyday documents, so incorrect handling would be noticeable but not widely disruptive."
  },
  {
    "section": "Code spans",
    "example": 334,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental Markdown feature; incorrect handling of backtick delimiters, trimming rules, or space vs non‑breaking space distinctions will change the displayed code content and frequently confuse readers in real-world docs and READMEs."
  },
  {
    "section": "Code spans",
    "example": 335,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental Markdown feature used heavily in docs, READMEs, and chat; incorrect handling of delimiter lengths, space-stripping, line-end normalization, backslashes, or precedence with other inline constructs would frequently misrender code and be confusing or misleading to readers."
  },
  {
    "section": "Code spans",
    "example": 336,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used frequently in docs, READMEs, and chat; incorrect handling of backtick delimiters, trimming rules, or precedence (code vs emphasis/links) would regularly break meaning and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 337,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational, frequently used Markdown feature; incorrect handling of backtick delimiters, trimming rules, or line-ending normalization will alter the literal code shown and reliably confuse or mislead readers."
  },
  {
    "section": "Code spans",
    "example": 338,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used widely; incorrect handling of backtick delimiters, space-stripping, escapes, or precedence would frequently misrender code snippets and confuse readers, so correct support is critical."
  },
  {
    "section": "Code spans",
    "example": 339,
    "weight": 1.0,
    "explanation": "Inline code spans are fundamental and ubiquitous in READMEs, docs, chat and AI output; incorrect handling of backtick-delimiter matching, space-stripping rules, line-ending treatment or precedence against emphasis/links would frequently misrender code and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 340,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental Markdown feature used frequently in docs and messages; incorrect handling of backtick delimiters, trimming rules, or precedence would regularly misrender code and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 341,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental and very common feature in documentation, READMEs and chat; behaviors like using multiple backticks to include backticks, the single-space trimming rule, literal backslashes, and precedence vs. emphasis/links materially affect readability and correctness, so incorrect rendering would frequently misrepresent code and confuse readers."
  },
  {
    "section": "Code spans",
    "example": 342,
    "weight": 1.0,
    "explanation": "Inline code spans are fundamental and very common in READMEs, documentation, and chat; incorrect handling of backtick delimiters, space stripping, line-ending treatment, or precedence with other inline constructs would frequently break meaning and be highly confusing to readers."
  },
  {
    "section": "Code spans",
    "example": 343,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used frequently in docs/READMEs/chat; incorrect handling of backtick delimiters, space trimming, or precedence would routinely break or mangle code fragments and be highly confusing."
  },
  {
    "section": "Code spans",
    "example": 344,
    "weight": 0.9,
    "explanation": "Inline code spans are a fundamental, frequently used Markdown feature and the example covers several subtle but important behaviors (multiple backtick delimiters, single-space trimming, whitespace treatment, precedence, literal backslashes). Incorrect handling would be common enough to confuse readers and break many docs, so correct support is highly important."
  },
  {
    "section": "Code spans",
    "example": 345,
    "weight": 0.8,
    "explanation": "Inline code spans are very common in documentation and chat; correct handling of backtick-delimiter matching, trimming rules, and interactions with autolinks/angle-bracket syntax affects many real-world code snippets and would noticeably confuse readers if implemented incorrectly, though some of the most exotic subcases are less frequent."
  },
  {
    "section": "Code spans",
    "example": 346,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used frequently in docs, READMEs, and chat; incorrect handling of delimiters, trimming rules, or precedence would often misrepresent code, links, or punctuation and thus be highly disruptive."
  },
  {
    "section": "Code spans",
    "example": 347,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational and very common Markdown feature; correct matching of backtick-delimiter lengths is essential to avoid frequent, confusing misrendering (e.g., treating code as literal backticks or as other constructs)."
  },
  {
    "section": "Code spans",
    "example": 348,
    "weight": 1.0,
    "explanation": "Inline code spans are a fundamental, frequently used feature in documentation, READMEs, and chat; incorrect handling of backtick delimiters, trimming rules, escapes, or precedence would frequently misrepresent code and confuse readers, so correct support is critical."
  },
  {
    "section": "Code spans",
    "example": 349,
    "weight": 1.0,
    "explanation": "Inline code spans are a foundational Markdown feature used frequently; incorrect handling of backtick delimiters, space stripping, precedence, or escaping will commonly misrender code fragments and change meaning, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 350,
    "weight": 1.0,
    "explanation": "Inline emphasis is a foundational Markdown feature used very frequently in documentation, READMEs, chat messages and AI output; rendering `*foo bar*` incorrectly would be highly noticeable and break expected semantics for many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 351,
    "weight": 0.5,
    "explanation": "This is a nuanced rule about delimiters adjacent to whitespace that prevents accidental emphasis; emphasis is foundational, so incorrect handling would be confusing, but this specific whitespace-adjacent case is an occasional edge-case rather than a widespread failure mode."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 352,
    "weight": 0.5,
    "explanation": "This tests a subtle punctuation-adjacent emphasis rule that appears occasionally (e.g., quotes, bibliographic entries) and can produce misleading emphasized text if implemented incorrectly, but it's a relatively specialized parsing edge case rather than a foundational Markdown feature."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 353,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis is a foundational feature; the exact delimiter rules (including that Unicode no-break spaces count as whitespace) affect whether asterisks render as emphasis or literal characters. Incorrect handling would commonly produce surprising formatting in real documents (copy/pasted/international text) and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 354,
    "weight": 0.7,
    "explanation": "This tests punctuation-adjacent emphasis parsing — a nuanced but important part of inline emphasis rules. While the exact pattern (*$* etc.) is not extremely common, incorrect handling would produce surprising emphasis around symbols (currency, punctuation) and break many realistic inline texts, so it's an important interoperability case."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 355,
    "weight": 1.0,
    "explanation": "Emphasis parsing is a foundational Markdown feature used widely; failing to support or mis-render *...* (including intraword cases like foo*bar*) would frequently break intended emphasis and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 356,
    "weight": 0.7,
    "explanation": "Intraword emphasis with `*` (e.g. 5*6*78) appears regularly in technical and math contexts and can easily be misinterpreted if rendered incorrectly; it’s not as foundational as headings or links but is common enough that wrong behavior is confusing and breaks many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 357,
    "weight": 1.0,
    "explanation": "Inline emphasis is a fundamental Markdown feature encountered frequently in docs, READMEs, and messages; failing to render `_foo bar_` correctly would be highly confusing and break common formatting expectations."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 358,
    "weight": 0.3,
    "explanation": "This tests a subtle underscore rule (an opening '_' followed by whitespace must not start emphasis). It's an edge-case of emphasis parsing that rarely appears in real-world writing and would only mildly confuse readers if handled incorrectly, so it's low-to-niche importance."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 359,
    "weight": 1.0,
    "explanation": "Emphasis parsing is a fundamental Markdown feature; the underscore-vs-asterisk rules determine whether common patterns (identifiers, filenames, quoted phrases) are rendered literally or as emphasis. Getting this wrong frequently breaks technical documentation and is confusing to readers, so correct handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 360,
    "weight": 0.9,
    "explanation": "Handling intraword underscores correctly is important because underscores are commonly used in variable names, filenames, and snake_case identifiers; incorrect emphasis on strings like foo_bar_ would frequently corrupt technical docs and READMEs and be confusing to readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 361,
    "weight": 0.6,
    "explanation": "Intraword underscores (e.g. version numbers, filenames, variable_names) are common in docs and code-related text; if a renderer treated 5_6_78 as emphasis it would corrupt or confuse important content — important to get right, but not as foundational as headings/links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 362,
    "weight": 0.7,
    "explanation": "Underscore-based intraword emphasis affects common real-world patterns (snake_case identifiers, filenames, or words with internal underscores) and would cause frequent, confusing accidental italicization; not as foundational as headings but important for typical technical/doc content."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 363,
    "weight": 1.0,
    "explanation": "This tests core emphasis parsing (that underscores within words do not form emphasis). Emphasis is a foundational Markdown feature and incorrect handling commonly mangles filenames, identifiers, and ordinary text, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 364,
    "weight": 1.0,
    "explanation": "Emphasis parsing is a foundational Markdown feature; this example tests subtle delimiter behavior with underscores next to punctuation (hyphen and parentheses). Incorrect handling would commonly break expected emphasis and confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 365,
    "weight": 1.0,
    "explanation": "This tests basic emphasis delimiter behavior (mismatched '_' vs '*'); emphasis/strong parsing is a foundational, very common feature, and getting it wrong would frequently produce misleading formatting in real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 366,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis parsing (including whitespace/punctuation rules for delimiters) is a foundational Markdown feature used widely; incorrect handling would frequently produce misleading formatting or visible literal delimiters and confuse readers, so correct behavior is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 367,
    "weight": 0.8,
    "explanation": "This checks correct handling of unmatched emphasis delimiters across a line break — a subtle but still fundamental emphasis rule. The exact pattern is uncommon, but getting it wrong can turn literal asterisks into spurious emphasis (or hide intended emphasis), which is confusing for readers, so correct support is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 368,
    "weight": 0.5,
    "explanation": "This tests a subtle delimiter-flanking rule for emphasis (whether certain adjacent asterisks are treated as markup). Correct handling prevents surprising emphasis in a few real-world cases, but the specific pattern is relatively uncommon compared with core features like headings, links, code blocks, and common list formatting."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 369,
    "weight": 0.9,
    "explanation": "Emphasis and nested emphasis are foundational Markdown features used frequently; misrendering nesting (as in *(*foo*)*) alters meaning and readability in many docs, so failure here would be highly impactful even if this exact pattern is less common than simple emphasis."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 370,
    "weight": 0.7,
    "explanation": "Intraword emphasis with `*` (e.g. `*foo*bar`) occurs fairly frequently in real-world writing and chat/README text; getting it wrong would produce visibly incorrect emphasis or stray markup, so it is important though slightly more specialized than the most foundational features."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 371,
    "weight": 0.5,
    "explanation": "This is a moderately important detail: underscores that are preceded or followed by whitespace should not form emphasis, and while the exact `_foo bar _` pattern is not extremely common, underscores are frequently used and mis-rendering (silent, unexpected emphasis) would be confusing to readers. It's not a foundational feature like headings/links, but getting these delimiting rules right avoids subtle, surprising output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 372,
    "weight": 0.3,
    "explanation": "This tests a fairly obscure edge-case of underscore-based emphasis adjacent to punctuation; while emphasis is foundational, this exact pattern (_(_foo)) is rare in real-world content, so incorrect rendering would be mildly confusing but not widely impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 373,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis is a foundational Markdown feature and nested emphasis (including underscore-based emphasis around punctuation) appears commonly; incorrect rendering changes meaning and would be confusing, so correct handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 374,
    "weight": 1.0,
    "explanation": "Treating intraword underscores as emphasis would frequently break real-world content (programming identifiers, filenames, snake_case, bibliographic entries) and produce confusing output; correct handling of _ vs * is part of basic emphasis behavior and thus critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 375,
    "weight": 0.8,
    "explanation": "Disallowing intraword emphasis with `_` prevents common constructs like snake_case identifiers, filenames, and many natural-language words from being incorrectly italicized; a renderer that treated internal underscores as emphasis would frequently break code samples and technical documentation and thus cause substantial confusion."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 376,
    "weight": 0.9,
    "explanation": "This is a core emphasis rule whose correct handling is important: incorrect underscore behavior will frequently break common patterns like snake_case variable/file names in READMEs, docs and chats, producing confusing or misleading output. While asterisk-based emphasis is more common, underscore semantics are still widely used and must be correct."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 377,
    "weight": 1.0,
    "explanation": "Emphasis parsing (including underscore rules around punctuation) is a fundamental Markdown feature used widely in real-world docs; incorrect handling would frequently break intent and confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 378,
    "weight": 1.0,
    "explanation": "Bold/strong emphasis is a fundamental Markdown feature used extremely frequently in documentation, READMEs, and messages; incorrect or missing support would be highly noticeable and confusing to readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 379,
    "weight": 1.0,
    "explanation": "Emphasis and strong-emphasis parsing is a foundational Markdown feature; getting delimiter-flanking rules (such as a space after the opening `**` preventing emphasis) wrong will commonly produce incorrect bolding or stray asterisks and thus materially confuse readers of many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 380,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis handling is a foundational Markdown feature used widely; getting delimiter-flanking rules wrong (especially around punctuation and quotes) will frequently produce surprising bold/italic or literal asterisks and thus confuse readers, so correct behavior is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 381,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (single * / _ and double ** / __) are fundamental Markdown features used ubiquitously; failing to parse basic patterns like foo**bar** (bold) or handling nesting rules correctly would frequently break readability and meaning in READMEs, docs, chat messages, and other real-world content."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 382,
    "weight": 1.0,
    "explanation": "Strong emphasis is a fundamental Markdown feature and the __...__ syntax is commonly used; failing to render double-underscore strong emphasis correctly would frequently break meaning and confuse readers in many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 383,
    "weight": 0.9,
    "explanation": "This tests a fundamental emphasis rule (that '__' followed by whitespace does not open strong emphasis). Emphasis/strong is a core Markdown feature and underscores are commonly used for bold; misrendering this case would change visible text and confuse readers, so it is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 384,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing is a foundational Markdown feature; handling of delimiters versus whitespace/newlines affects whether text is rendered as literal underscores or as (strong) emphasis, so incorrect behavior would commonly confuse readers and break many real-world docs."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 385,
    "weight": 0.7,
    "explanation": "This is an important but slightly specialized rule about how underscores interact with surrounding punctuation/whitespace to open/close emphasis; many real-world texts (variable names, file_names, quoted phrases) use underscores and mis-rendering would produce misleading or spurious emphasis, though it's not as universally critical as headings or links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 386,
    "weight": 0.9,
    "explanation": "Underscored identifiers (foo__bar__) are common in code, READMEs, and docs; incorrectly treating __ inside words as strong emphasis would misrender variable names and harm readability. This is a core nuance of emphasis parsing (preventing intraword __ emphasis), so failures are high-impact."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 387,
    "weight": 0.8,
    "explanation": "This checks that double underscores do not create strong emphasis inside words/numbers (so identifiers like variable_names or dunder methods and numeric sequences aren’t incorrectly bolded). Many READMEs and docs use underscores in identifiers, so getting this wrong would commonly and confusingly mangle meaning."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 388,
    "weight": 0.7,
    "explanation": "This tests that underscores do not create emphasis inside words (here Cyrillic); emphasis handling is a foundational feature and mis-parsing intraword underscores can break multilingual text and technical identifiers, so incorrect rendering would be noticeably confusing even if this exact Cyrillic case is somewhat specialized."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 389,
    "weight": 0.9,
    "explanation": "Emphasis and strong emphasis are fundamental Markdown features; correct handling of nesting (including __ inside __) is important for real-world docs, READMEs, and chat output. While this exact underscore-nesting pattern is less common than simple bold/italic, incorrect rendering would be confusing and break author intent, so robust support is highly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 390,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis parsing is a fundamental feature of Markdown; incorrect handling of underscore-based strong emphasis (especially around punctuation/hyphens) is common enough in real documents (READMEs, docs, chat) and would produce confusing, visibly incorrect output, so support is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 391,
    "weight": 1.0,
    "explanation": "Emphasis and strong-emphasis parsing is a fundamental Markdown feature used widely in documentation, READMEs, chat and AI output; getting delimiter-flanking rules wrong leads to frequent, confusing mis-formatting (unexpected bold/italics or literal asterisks), so incorrect handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 392,
    "weight": 0.5,
    "explanation": "This is a nuanced delimiter-flanking rule about whether '**(' should start strong emphasis; it's not extremely common in everyday Markdown but does occur in technical/parenthetical contexts and incorrect handling can produce confusing formatting, so it's moderately important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 393,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis (and their nesting) are fundamental Markdown features used frequently in documentation and prose; incorrect handling would visibly break text formatting and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 394,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nesting and intraword rules) are foundational Markdown features used ubiquitously in real-world text; incorrect handling changes meaning and can be confusing or misleading, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 395,
    "weight": 1.0,
    "explanation": "Nested emphasis/strong-emphasis is a core, commonly used Markdown feature (e.g., emphasis inside bold or quoted text); incorrect rendering would break basic formatting and confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 396,
    "weight": 1.0,
    "explanation": "Strong/emphasis parsing is a foundational Markdown feature; adjacent spans like **foo**bar occur commonly (e.g., README text) and incorrect handling would break basic formatting and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 397,
    "weight": 0.8,
    "explanation": "Emphasis/strong emphasis is a foundational Markdown feature; incorrect handling of delimiter rules (especially the subtle underscore vs asterisk and spacing cases) will cause visible and confusing formatting errors. The exact spacing edge case here is somewhat less common than basic emphasis mistakes but still appears regularly enough (typos, bibliographic entries, prose) to make incorrect rendering important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 398,
    "weight": 0.9,
    "explanation": "This exercises nuanced rules for underscore-delimited emphasis/strong emphasis; emphasis parsing is a foundational feature, and getting underscore behavior wrong can frequently produce confusing output (even if this exact delimiter combination is somewhat less common)."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 399,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis and their nesting are fundamental Markdown features used frequently in real-world docs; getting underscore/asterisk delimiter rules and punctuation/parenthesis interactions wrong will visibly change meaning and confuse readers, so correct handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 400,
    "weight": 0.7,
    "explanation": "Underscore-based intraword emphasis affects common real-world text like programming identifiers and filenames; getting it wrong can visibly mangle variable names and be confusing. It's not as fundamental as headings or links, but it's an important, frequently-encountered nuance of emphasis parsing."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 401,
    "weight": 0.6,
    "explanation": "This example checks that underscores do not create (strong) emphasis when used intraword (here with Cyrillic text). Incorrect handling would frequently mangle identifiers, filenames, or non‑Latin words and be confusing, but it is not as foundational as headings/links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 402,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis parsing is a foundational Markdown feature and underscores appear frequently in identifiers, filenames, and prose; getting this delimiter-run behavior wrong will often produce visibly incorrect or misleading formatting. The specific nuance (double-underscore wrapping with internal double-underscores treated as literal) is common enough that incorrect rendering noticeably breaks real-world docs and READMEs."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 403,
    "weight": 1.0,
    "explanation": "Double-underscore strong emphasis is a fundamental Markdown feature and is common in real documents; failing to render __bold__ (or rendering literal underscores) would be highly visible and could mislead readers about emphasis/meaning."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 404,
    "weight": 1.0,
    "explanation": "Emphasis containing links is a common, core inline pattern in documentation and READMEs; getting the nesting/precedence wrong changes meaning and appearance and would be highly confusing, so correct handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 405,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis is a foundational Markdown feature; incorrect handling of delimiter runs or failure to allow emphasis to span a line break (as in this example) would frequently break intended italics/bold and be highly confusing in many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 406,
    "weight": 1.0,
    "explanation": "This tests nested emphasis (italic containing bold) using underscores — a very common, foundational inline formatting pattern; incorrect handling would noticeably break basic text emphasis and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 407,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing (including nesting rules and the underscore vs asterisk intraword behavior) is a foundational, very common feature: incorrect handling will frequently break formatting and meaning (e.g., identifiers like foo_bar_baz or nested emphasis), so correct rendering is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 408,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing (including delimiter-run rules, nesting, and underscore vs asterisk behavior) is a foundational Markdown feature used everywhere; getting these rules wrong can frequently change meaning and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 409,
    "weight": 1.0,
    "explanation": "Parsing of emphasis and strong emphasis (including nesting, intraword rules, and differing '*' vs '_' behavior) is a foundational and very common Markdown feature; incorrect rendering will frequently produce confusing or misleading output in READMEs, docs and chat, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 410,
    "weight": 1.0,
    "explanation": "Emphasis and strong nesting are foundational Markdown features used very frequently in real-world documents (readmes, articles, chat), so incorrect handling (e.g., failing to nest ** inside * ) would be highly confusing and break common formatting expectations."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 411,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nested/overlapping uses like *foo**bar**baz*) are fundamental and very common in real-world Markdown; incorrect handling would noticeably break formatting and confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 412,
    "weight": 1.0,
    "explanation": "This tests core emphasis/strong-emphasis parsing (overlapping/nested asterisks), a foundational Markdown feature used frequently; incorrect handling would commonly mislead readers and break many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 413,
    "weight": 1.0,
    "explanation": "This tests nested emphasis/strong using combined delimiter runs — emphasis and strong emphasis (and their nesting) are foundational Markdown features used frequently in documentation and messages; getting them wrong commonly changes meaning and confuses readers, so incorrect rendering would be highly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 414,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (and their nesting) are foundational Markdown features used frequently in READMEs, docs and chat; getting delimiter rules wrong leads to visibly incorrect and potentially misleading formatting, so failure to handle this case is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 415,
    "weight": 0.9,
    "explanation": "Emphasis and strong emphasis are foundational Markdown features and mixed/nested delimiter sequences (like *foo**bar***) occur often enough in real documents to matter; getting the delimiter-matching and precedence rules wrong produces visibly incorrect formatting that confuses readers and breaks common authoring patterns."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 416,
    "weight": 1.0,
    "explanation": "Nested emphasis using combined delimiters (e.g., ***bold+italic***) is a common, foundational Markdown pattern for bold/italic text; incorrect handling changes visual meaning and is likely to confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 417,
    "weight": 0.9,
    "explanation": "Emphasis and strong emphasis parsing is a fundamental Markdown feature used widely; the delimiter-run rules determine whether common * and ** (and nested combinations like ***, intraword underscores, or long runs of asterisks) render as intended. Mistakes here cause obvious visual/meaning errors or stray literal markers, so incorrect support would be highly impactful even if the most arcane long-run cases are rare."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 418,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nesting and delimiter-run rules) are foundational Markdown features used frequently; getting them wrong produces visible, confusing HTML (or loss of intended emphasis) and breaks common documents, so correct behavior is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 419,
    "weight": 1.0,
    "explanation": "This tests the common and foundational interaction of emphasis with links (emphasis nested inside links and links grouping tighter than emphasis). Such patterns appear frequently in documentation and chat, and incorrect handling would visibly alter meaning and confuse readers, so correct rendering is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 420,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis handling is a foundational Markdown feature used frequently; failing to treat asterisks followed by whitespace as non-delimiters (producing empty/incorrect emphasis) would lead to obvious, confusing misrendering of common text."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 421,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis parsing is a fundamental Markdown feature used widely; the rule that `****` does not form an empty strong-emphasis span (and should be treated as literal asterisks) prevents spurious/empty formatting and preserves author intent—incorrect handling would be common and visibly confusing in real-world docs and chats."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 422,
    "weight": 1.0,
    "explanation": "Emphasis containing links (or vice versa) is extremely common in docs and READMEs; rule 17 is foundational for correct nesting so misrendering would frequently produce broken or misleading HTML and confusing output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 423,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis are foundational Markdown features and allowing them to span lines within a paragraph is common; failing to support this (or rendering it incorrectly) would frequently break intended formatting and be immediately confusing to readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 424,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis are foundational Markdown features used frequently (bold and italic, including nesting with _ and __); incorrect handling (especially of underscore rules and nesting) changes meaning and is highly noticeable and confusing to readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 425,
    "weight": 1.0,
    "explanation": "Emphasis and strong-emphasis parsing is foundational and ubiquitous in real-world Markdown; incorrect handling of nesting, intraword underscores/asterisks, or delimiter-run rules would frequently break intended formatting and confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 426,
    "weight": 0.5,
    "explanation": "This is a moderately useful edge case: nested strong/emphasis with multiple underscores and the delimiter-run rules appear occasionally (in READMEs, bibliographies, or complex inline markup). Misrendering would be confusing to readers who rely on precise nesting, but it is not as foundational as headings, links, or code blocks."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 427,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis parsing is a fundamental Markdown feature; incorrect handling of delimiter runs and nesting (as in this example) will frequently misrender bold/italic text and confuse readers, so correct behavior is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 428,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nesting and the * vs _ rules) are fundamental and ubiquitous in Markdown; incorrect handling causes widespread, visible misrendering and reader confusion, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 429,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nesting and mixed use of * and _) are fundamental Markdown features used frequently in documentation, READMEs, chat and generated text; incorrect handling leads to visibly wrong bold/italic rendering and can confuse readers, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 430,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nested combinations) are fundamental Markdown features used frequently; getting delimiter-run/nesting rules wrong will visibly change formatting and meaning in many real-world documents, so correct handling is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 431,
    "weight": 1.0,
    "explanation": "Emphasis and strong emphasis (including nesting and delimiter-run rules) are foundational Markdown features used often in READMEs, docs, and messages; incorrect handling yields widespread, confusing misrendering of common text (e.g., nested * and **), so failing this is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 432,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing (including nesting and intraword rules) is a foundational Markdown feature appearing frequently in READMEs, docs, chat and AI output; incorrect handling changes meaning and is highly visible/confusing to readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 433,
    "weight": 1.0,
    "explanation": "This tests the interaction of strong/emphasis with links (rule 17) — a very common pattern (bold text containing a linked emphasized word) in docs and READMEs; incorrect handling would misnest tags or break links/emphasis and be visibly misleading, so it is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 434,
    "weight": 0.5,
    "explanation": "This tests a subtle but fundamental emphasis rule (that `__` alone does not form an empty strong-emphasis span). It's an occasional real-world pattern and, while not as common as basic emphasis/heading support, incorrect handling would produce surprising empty tags or strip literal underscores, so correct behavior is moderately important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 435,
    "weight": 0.7,
    "explanation": "Emphasis/strong-emphasis parsing is foundational and appears frequently; the specific four-underscore/empty-strong-edge case is uncommon but an incorrect implementation can produce empty/malformed <em>/<strong> tags or strip literal underscores, leading to confusing rendering, so it is important to get right."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 436,
    "weight": 0.6,
    "explanation": "Emphasis/strong parsing is a foundational feature, so mis-handling delimiter-run edge cases can produce visibly wrong inline formatting; however this specific case (trailing `***` after text) is a relatively uncommon corner that would be annoying but not catastrophic in most docs."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 437,
    "weight": 0.8,
    "explanation": "This tests emphasis combined with backslash-escaped delimiters — emphasis is a foundational feature and escapes are commonly used to include literal asterisks; getting this wrong can produce confusing text or incorrect emphasis, though the exact pattern is a somewhat specialized edge-case."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 438,
    "weight": 1.0,
    "explanation": "Emphasis parsing is a foundational Markdown feature used everywhere; getting delimiter behavior (including mixed `*`/`_` cases like `*_ *`) wrong will visibly alter text and confuse readers, so incorrect rendering would be highly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 439,
    "weight": 0.8,
    "explanation": "Correct handling of emphasis delimiters is a core Markdown feature; sequences like \"foo *****\" are uncommon but not rare and must not be misparsed into unintended <em>/<strong> spans. Getting this wrong leads to visible formatting errors and reader confusion, so support for the delimiter-sum rules is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 440,
    "weight": 0.7,
    "explanation": "This exercises escaping an asterisk inside strong emphasis — a subtle but important facet of emphasis parsing. Emphasis/strong emphasis is fundamental and incorrect handling (showing stray backslashes or wrong emphasis) can be confusing, and the need to display literal '*' within formatted text occurs often enough to be impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 441,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing is a foundational Markdown feature; incorrect handling of nested or mixed delimiters (like **_**) would frequently produce wrong or confusing formatting in real-world docs and README files, so correct support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 442,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing is a foundational Markdown feature; patterns like mismatched delimiters (e.g. **foo* → *<em>foo</em>) occur frequently and are easy to mistype, so incorrect handling would produce misleading or broken text in many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 443,
    "weight": 0.7,
    "explanation": "This exercises how unmatched/mixed emphasis delimiters are handled — a somewhat common and subtle case in real authoring. Emphasis parsing is a foundational feature, so incorrect handling of stray or unbalanced asterisks can noticeably change rendering and confuse readers, though it’s not as critical as headings or links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 444,
    "weight": 0.9,
    "explanation": "This tests core emphasis/strong-emphasis delimiter behavior (how runs of * combine/split) — a foundational inline feature that appears frequently in real docs and READMEs; getting it wrong changes text semantics and is noticeably confusing, so correct handling is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 445,
    "weight": 0.6,
    "explanation": "Emphasis/strong-emphasis parsing is a foundational Markdown feature and many documents use *, ** and nested patterns; the exact delimiter-run rules are somewhat subtle and relatively uncommon but incorrect handling can change meaning and confuse readers. Therefore it's important though this particular complex delimiter-edge case is less frequent than basic emphasis/links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 446,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing is a fundamental Markdown feature used frequently; mishandling mixed delimiters (e.g. **foo***) will change which text is emphasized and whether a trailing '*' is rendered literally, leading to visibly incorrect and confusing output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 447,
    "weight": 0.8,
    "explanation": "This tests subtle but common emphasis parsing behavior (how extra delimiter characters are left literal), and while the exact pattern is somewhat contrived many real-world inputs mix `*` runs — incorrect handling can produce surprising emphasis or drop literal asterisks, which is confusing since emphasis is a fundamental Markdown feature."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 448,
    "weight": 0.5,
    "explanation": "This tests a specific delimiter-edge case (three underscores after a word at line end) ensuring they remain literal rather than forming emphasis or a horizontal rule. It's not very common in real documents, but misrendering would be moderately confusing, so correctness is moderately important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 449,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing (including underscore rules and backslash escapes) is a fundamental Markdown feature used widely in real documents; incorrect handling will frequently change visible text (especially variable names and escaped underscores) and confuse readers, so noncompliance is highly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 450,
    "weight": 0.9,
    "explanation": "Emphasis/strong emphasis parsing is a foundational Markdown feature; while the exact pattern `_ * _` is a somewhat subtle edge of the delimiter rules, incorrect handling would frequently produce surprising/misleading formatting in ordinary documents (mixed delimiters and nested emphasis occur often enough) and thus is highly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 451,
    "weight": 0.3,
    "explanation": "This tests a fairly niche delimiter-run edge case (a run of underscores at the end of a word/line that should be literal). Misrendering would be mildly confusing in specific texts, but it is not a foundational Markdown feature and is uncommon in real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 452,
    "weight": 0.9,
    "explanation": "Emphasis/strong-emphasis parsing (including underscore vs asterisk behavior and backslash escapes) is a foundational feature that appears often (IDs, filenames, inline text); incorrect handling leads to confusing or misleading formatting, so this subtle case is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 453,
    "weight": 1.0,
    "explanation": "This exercises core emphasis/strong-emphasis delimiter rules (underscore vs asterisk interaction). Emphasis and strong emphasis are foundational and common; incorrect handling would frequently produce wrong bold/italic output and confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 454,
    "weight": 0.5,
    "explanation": "This tests a subtle underscore-delimiter rule (mixed-length runs and intraword behavior) that many documents avoid or use `*` instead of `_`; incorrect rendering can leave stray underscores or wrong emphasis and confuse readers, but it is an infrequent, nuanced case rather than a foundational failure."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 455,
    "weight": 1.0,
    "explanation": "Emphasis parsing is foundational — underscores are commonly used and the subtle rule about unmatched/mixed delimiters (leaving a trailing literal underscore) must be handled correctly to avoid widespread, confusing mis-rendering of ordinary text."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 456,
    "weight": 0.9,
    "explanation": "This tests emphasis/strong-emphasis delimiter behavior — a foundational feature that appears frequently (emphasis, nested emphasis, underscores vs asterisks). Incorrect handling (especially of _ vs * and triple delimiters) can visibly change meaning and confuse readers, so it is highly important even if the exact triple-underscore pattern is a bit niche."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 457,
    "weight": 0.9,
    "explanation": "This tests core emphasis parsing rules (delimiter runs, left/right-flanking, and underscore-specific behavior). While the exact sequence is a bit uncommon, emphasis/strong-emphasis is foundational — getting these rules wrong changes visible styling and can be confusing, so correct handling is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 458,
    "weight": 0.9,
    "explanation": "Emphasis/strong emphasis parsing is a foundational Markdown feature, and this example exercises a common delimiter-run edge (mixed-length underscores) that many documents hit (underscores in text, README snippets). Incorrect rendering would produce misleading emphasis or stray underscores and break user expectations, so correct handling is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 459,
    "weight": 0.6,
    "explanation": "This tests subtle delimiter-run behavior for underscores (how many underscores are consumed as emphasis vs left literally), which is part of core emphasis handling and can lead to visually confusing output if implemented incorrectly, but the exact pattern (_foo____) is a somewhat uncommon edge case in typical documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 460,
    "weight": 1.0,
    "explanation": "Critical: emphasis and strong emphasis are foundational, very commonly used inline formatting in virtually all Markdown documents (READMEs, docs, chat, AI output), so failing to render **foo** as <strong>foo</strong> would break readability and meaning across a vast number of real-world texts."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 461,
    "weight": 0.9,
    "explanation": "Emphasis and nesting rules are a core Markdown feature used frequently in docs, READMEs, and chat; incorrect handling of nested '*' vs '_' changes meaning and is noticeably confusing, so correct support is highly important (slightly below absolute critical features like links/headings)."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 462,
    "weight": 1.0,
    "explanation": "Emphasis/strong emphasis (e.g., __bold__ or **bold**) is a fundamental Markdown feature used widely in READMEs, docs, and chat; failing to render it correctly would frequently break intent and greatly confuse readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 463,
    "weight": 1.0,
    "explanation": "Emphasis parsing (including interactions between * and _) is a foundational Markdown feature used widely in docs, READMEs, and chat; incorrect handling of nesting or delimiter rules would frequently produce misleading or broken formatting, so full support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 464,
    "weight": 1.0,
    "explanation": "Emphasis/strong-emphasis parsing is foundational to Markdown and appears frequently; incorrect handling of delimiter runs (as in ****foo****) would break many common emphasis patterns and produce confusing or visible raw delimiters, so full support is critical."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 465,
    "weight": 0.5,
    "explanation": "This tests a specific nuance of emphasis delimiter runs (nested strong emphasis with repeated underscores). Basic emphasis/strong parsing is important, but this exact pattern (four underscores producing nested <strong> tags) is relatively uncommon — misrendering would be mildly confusing but not widely disruptive."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 466,
    "weight": 0.9,
    "explanation": "Emphasis/strong parsing is a foundational Markdown feature used very frequently; incorrect handling of delimiter runs and nesting will cause many real-world documents to render wrong or confusing emphasis. The specific six-asterisk triple-nesting is somewhat uncommon but exercises core rules, so correct support is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 467,
    "weight": 0.9,
    "explanation": "Triple-delimiter nesting (e.g., ***bold+italic***) is a common, practical pattern in READMEs, docs and chat, and correct handling of delimiter runs is part of basic emphasis semantics. Incorrect rendering would frequently change HTML structure and could alter styling or accessibility expectations, so getting this right is important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 468,
    "weight": 0.3,
    "explanation": "This is a niche, corner-case behavior about how long runs of underscores nest into emphasis/strong according to detailed delimiter rules. Sequences like five underscores around a word are uncommon in real-world content, so incorrect rendering is confusing when encountered but not critical for most documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 469,
    "weight": 0.9,
    "explanation": "Emphasis/strong parsing is a foundational Markdown feature used widely; although the specific overlapping case (*foo _bar* baz_) is somewhat uncommon, incorrect handling produces visibly wrong emphasis and can confuse readers, so correct behavior is highly important."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 470,
    "weight": 0.9,
    "explanation": "Emphasis and strong-emphasis parsing is a foundational Markdown feature and mixed/nested uses of * and _ occur frequently enough in real docs, READMEs and chat that incorrect rendering would be visibly confusing and change meaning; the detailed delimiter rules are essential for predictable, interoperable output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 471,
    "weight": 1.0,
    "explanation": "This tests core emphasis/strong-emphasis disambiguation (overlapping/mismatched delimiters) — a fundamental Markdown behavior; getting it wrong would frequently produce surprising/incorrect emphasis or stray literal asterisks, confusing readers and breaking many real-world documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 472,
    "weight": 0.7,
    "explanation": "This tests subtle but common emphasis/strong-emphasis parsing rules — emphasis is a foundational feature and nested/overlapping delimiters appear in real writing; incorrect handling produces visible stray markers or wrong nesting that confuse readers, though it is a more specific/osmotic parsing nuance rather than the single-most-critical Markdown feature."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 473,
    "weight": 0.9,
    "explanation": "This tests that links (and other inline elements) take precedence over emphasis so delimiters adjacent to a link are treated as literal rather than creating crossed emphasis; since links and emphasis are both extremely common, getting this wrong leads to frequently visible broken/misformatted text and confusing output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 474,
    "weight": 0.8,
    "explanation": "Emphasis handling (especially with _ vs * and interaction with links) is a common pattern in real docs (filenames, identifiers, bibliographies) and is core Markdown behavior; getting this wrong can produce confusing or broken emphasis/link text, so incorrect rendering is fairly impactful."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 475,
    "weight": 0.5,
    "explanation": "This tests that HTML tags (and their attributes) group more tightly than emphasis so the leading '*' and the '*' in the attribute remain literal. Raw HTML in Markdown is fairly common, but this exact interaction is uncommon; mis-rendering would be confusing (e.g., creating spurious emphasis or broken output) but not as critical as core features like headings or links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 476,
    "weight": 0.7,
    "explanation": "This tests that inline HTML (tags/attributes) binds more tightly than emphasis so that asterisks inside an HTML tag or attribute aren’t parsed as emphasis. The exact pattern is uncommon, but the general rule is important and frequently affects real-world markup (links, images, code/HTML embedding); incorrect handling can produce malformed HTML or confusing output."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 477,
    "weight": 0.7,
    "explanation": "This tests the interaction of emphasis rules with raw HTML (underscores inside an HTML tag/attribute). While somewhat specific, HTML fragments and underscores in attributes/URLs appear fairly often; mishandling this can produce malformed HTML or surprising emphasis, breaking links and confusing readers."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 478,
    "weight": 0.8,
    "explanation": "This tests that inline code spans group more tightly than emphasis (so `*a `*`*` becomes <em>a <code>*</code></em>); mixing emphasis and inline code appears fairly often in docs/READMEs and misrendering would be confusing (visible backticks or wrong nesting), so it is important though slightly more specialized than bare headings/links."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 479,
    "weight": 0.7,
    "explanation": "Inline code inside emphasized text is a fairly common pattern (e.g., docs and READMEs) and rule 17 affects parsing priority; getting this wrong can visibly break formatting and confuse readers, so correct handling is important though not as critical as core block-level features."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 480,
    "weight": 0.5,
    "explanation": "This is a somewhat uncommon edge case (emphasis delimiters overlapping an angle-bracket autolink) but misrendering can break or mangle URLs and confuse readers, so correct handling is moderately important even if it doesn't appear in most documents."
  },
  {
    "section": "Emphasis and strong emphasis",
    "example": 481,
    "weight": 0.5,
    "explanation": "This tests a subtle interaction between underscore-delimited emphasis and autolinks (ensuring the link is parsed and the surrounding underscores remain literal). Misrendering can be confusing for readers when underscores around links are swallowed as emphasis, but the pattern is a relatively niche corner case compared with core features like headings, code blocks, or inline links in general."
  },
  {
    "section": "Links",
    "example": 482,
    "weight": 1.0,
    "explanation": "Inline links (URI plus optional title) are a fundamental Markdown feature used widely in READMEs, docs, and chats; failing to support or rendering them incorrectly would break navigation and lose important semantic/title information, so this is critical."
  },
  {
    "section": "Links",
    "example": 483,
    "weight": 1.0,
    "explanation": "Inline links are a foundational and extremely common Markdown feature (READMEs, docs, chats); failing to render them correctly would severely harm navigation and comprehension of documents."
  },
  {
    "section": "Links",
    "example": 484,
    "weight": 0.5,
    "explanation": "Allowing an empty link text is an uncommon but legitimate form of inline link; it's an edge-case of a foundational feature (links). Incorrect handling is usually low-visibility (the anchor has no visible text) but can break link semantics or generated HTML in some workflows, so moderate importance is appropriate."
  },
  {
    "section": "Links",
    "example": 485,
    "weight": 0.5,
    "explanation": "An empty inline destination like [link]() is a legitimate, occasionally used pattern (often as a placeholder); incorrect handling would be confusing but not catastrophic. Links are foundational, but the empty-destination case is a specific edge of link parsing rather than a broadly pervasive pattern."
  },
  {
    "section": "Links",
    "example": 486,
    "weight": 0.2,
    "explanation": "Allowing an empty destination inside angle brackets is a legal but rare edge case; most documents don't use empty-URL links, so incorrect rendering is unlikely to cause major confusion though it could be mildly surprising."
  },
  {
    "section": "Links",
    "example": 487,
    "weight": 0.3,
    "explanation": "An inline link with both empty text and empty destination is a niche edge case that rarely appears in real documents; while link handling is foundational, failing this specific case would be unlikely to confuse most readers though it would break spec compliance and some test suites."
  },
  {
    "section": "Links",
    "example": 488,
    "weight": 0.7,
    "explanation": "This tests the common-link rule that inline destinations cannot contain unescaped spaces (must use <...>); people sometimes write URLs with spaces or forget angle brackets, and misrendering would produce unexpected links and confuse readers, so it's an important (but not foundational) behavior to get right."
  },
  {
    "section": "Links",
    "example": 489,
    "weight": 0.7,
    "explanation": "Accepting angle-bracketed destinations with spaces is a reasonably common need (file paths, local URLs, author mistakes) and incorrect handling can produce broken or confusing links; it's not as fundamental as headings/inline code but is an important correctness case for link parsing."
  },
  {
    "section": "Links",
    "example": 490,
    "weight": 0.3,
    "explanation": "This tests that inline link destinations cannot contain line endings — a relatively rare/edge-case pattern. Incorrectly treating such multi-line text as a link would be confusing but is uncommon, so the impact is modest."
  },
  {
    "section": "Links",
    "example": 491,
    "weight": 0.3,
    "explanation": "This is an uncommon edge case (a line break inside an angle-bracket link destination). Most real-world documents don't put newlines inside <...> URLs, so failing this rule would rarely affect readers; when it does occur it may be mildly confusing but is not foundational."
  },
  {
    "section": "Links",
    "example": 492,
    "weight": 0.7,
    "explanation": "This is an important link-parsing edge case: it ensures that an angle-bracket-enclosed destination is recognized correctly even when followed by extra parentheses. Incorrect handling would commonly break URLs containing parentheses or mis-parse nearby punctuation, producing broken or confusing links in real documents."
  },
  {
    "section": "Links",
    "example": 493,
    "weight": 0.3,
    "explanation": "This is a niche edge case about escaping a closing > inside an angle-bracket link destination; it rarely appears in real-world content. Misrendering could confuse a reader in specific cases but is not critical for most documents."
  },
  {
    "section": "Links",
    "example": 494,
    "weight": 0.3,
    "explanation": "This exercises a fairly niche parsing edge—how angle-bracketed link destinations interact with parentheses and unmatched brackets. Correct handling is useful, but such malformed or unusual combinations are uncommon in real-world content, so incorrect rendering would be surprising but not broadly impactful."
  },
  {
    "section": "Links",
    "example": 495,
    "weight": 0.7,
    "explanation": "Links are a fundamental feature and parentheses appear in real URLs fairly often; incorrectly handling backslash-escaped parentheses can break or mis-target links and confuse readers, so correct handling is important though somewhat more specific than the most basic link cases."
  },
  {
    "section": "Links",
    "example": 496,
    "weight": 1.0,
    "explanation": "Supporting balanced parentheses inside inline link destinations is a core link-parsing behavior: URLs often contain parentheses (e.g., wiki or function-like paths), and failing to handle them will commonly produce broken links and misleading rendering, so correct handling is critical."
  },
  {
    "section": "Links",
    "example": 497,
    "weight": 0.6,
    "explanation": "This is an inline-link parsing edge case (missing closing parenthesis) that appears occasionally because URLs and parenthetical text are common; mis-parsing can produce incorrect or broken links or swallow text, which is confusing, though it is not as fundamental as basic heading/paragraph handling."
  },
  {
    "section": "Links",
    "example": 498,
    "weight": 0.8,
    "explanation": "This checks backslash-escaping of parentheses inside inline link destinations — a common need because many real URLs contain parentheses (e.g., Wikipedia links). If a renderer mishandles this, link targets will be wrong or broken, which is confusing and damaging even though it's a narrower detail of link parsing."
  },
  {
    "section": "Links",
    "example": 499,
    "weight": 0.8,
    "explanation": "This tests allowing parentheses inside angle-bracket-delimited link destinations — a fairly common real-world case (URLs with parentheses). Incorrect handling will often break or truncate links and confuse readers, so it is an important capability even if somewhat specific to link parsing nuances."
  },
  {
    "section": "Links",
    "example": 500,
    "weight": 0.7,
    "explanation": "This tests handling of backslash-escapes and parentheses inside inline link destinations — a somewhat common real-world case (URLs often contain parentheses) and a core link-related behavior; incorrect handling can easily break or truncate links and confuse readers."
  },
  {
    "section": "Links",
    "example": 501,
    "weight": 1.0,
    "explanation": "This tests basic inline link destinations (fragment-only links, fragments on absolute URLs, and queries+fragments), a very common pattern in docs and READMEs; incorrect handling would break navigation and is a fundamental link feature."
  },
  {
    "section": "Links",
    "example": 502,
    "weight": 0.5,
    "explanation": "Backslashes in link destinations appear reasonably often (e.g., Windows paths) and mishandling can produce broken or incorrect URLs, but this is a specific parsing/escaping detail rather than a foundational feature, so it is moderately important."
  },
  {
    "section": "Links",
    "example": 503,
    "weight": 0.5,
    "explanation": "This checks parsing of HTML entities inside link destinations and subsequent normalization/percent-encoding; not extremely common but seen in internationalized URLs or copy-pasted content, and mishandling can alter or break link targets, so it's moderately important."
  },
  {
    "section": "Links",
    "example": 504,
    "weight": 0.3,
    "explanation": "This is a fairly niche parsing edge case (title-only text inside parentheses being treated as the destination); it rarely occurs in well-formed real-world Markdown and misrendering is mildly confusing but not critical to most documents."
  },
  {
    "section": "Links",
    "example": 505,
    "weight": 0.8,
    "explanation": "This checks support for the three accepted title delimiters (\", ', and ()) in inline links—something that appears regularly in READMEs and docs; if mishandled a renderer may drop titles or mis-parse links, causing user-visible loss of information, so it is important though slightly less foundational than basic link support."
  },
  {
    "section": "Links",
    "example": 506,
    "weight": 0.5,
    "explanation": "This tests escaping and entity handling inside an inline link title — a fairly common but not ubiquitous pattern. Incorrect rendering would be noticeable (backslashes or unescaped quotes breaking the HTML attribute or showing wrong tooltip) but it’s not as fundamental as basic link parsing or block structure."
  },
  {
    "section": "Links",
    "example": 507,
    "weight": 0.3,
    "explanation": "This is a niche Unicode-separator edge case: it ensures only ASCII space/tab/line-ending separate destination and title, so a non‑breaking space becomes part of the URL. It rarely appears in normal docs (mostly from copy/paste or rich‑text sources) but can cause subtle URL/title differences if implemented incorrectly."
  },
  {
    "section": "Links",
    "example": 508,
    "weight": 0.5,
    "explanation": "This tests handling of unescaped quotes inside a double‑quoted link title — an occasional real‑world pattern (authors quoting text inside titles) where incorrect parsing can change whether text becomes a link or plain text. It's not foundational but is a moderately useful/occasionally encountered edge case that can be confusing if rendered wrong."
  },
  {
    "section": "Links",
    "example": 509,
    "weight": 0.7,
    "explanation": "Parsing inline link titles (including single-quoted titles containing quotes) and properly HTML-escaping them is important: titles are reasonably common and incorrect handling can lose metadata or produce broken/unsafe HTML, though this is slightly less critical than basic link recognition itself."
  },
  {
    "section": "Links",
    "example": 510,
    "weight": 0.9,
    "explanation": "This tests a core inline-link parsing rule (allowing spaces and up to one line ending between destination and title). Links are fundamental and authors sometimes put a newline or extra spaces for readability; incorrect handling would frequently break or misinterpret valid links and be noticeably confusing."
  },
  {
    "section": "Links",
    "example": 511,
    "weight": 0.5,
    "explanation": "This tests that a space between the link text and the opening parenthesis prevents inline-link parsing. People usually write links correctly but occasionally include a space; getting this wrong can produce or omit links unexpectedly, which is confusing but not catastrophic."
  },
  {
    "section": "Links",
    "example": 512,
    "weight": 0.6,
    "explanation": "Nested balanced brackets in link text are a reasonably common real-world pattern (especially in technical docs) and misparsing can break links or display confusing text; it’s a link-related edge case — not as foundational as simple links but still important to render correctly."
  },
  {
    "section": "Links",
    "example": 513,
    "weight": 0.3,
    "explanation": "This is a niche edge case about disallowing intervening characters between link text and the following '(' so the text remains literal; it’s uncommon in real documents, though mis-parsing could produce an unexpected link and be mildly confusing."
  },
  {
    "section": "Links",
    "example": 514,
    "weight": 0.3,
    "explanation": "This is a fairly niche edge case about how unbalanced brackets and nested links are parsed; it doesn't occur often in typical docs, and while incorrect rendering could be mildly confusing (breaking or swallowing the inner link), it's not critical compared with core link/heading behavior."
  },
  {
    "section": "Links",
    "example": 515,
    "weight": 0.5,
    "explanation": "This tests escaping an open bracket inside link text — a moderately common need when link labels include literal brackets. Incorrect rendering can break the link or misdisplay the visible text, but it's not a foundational feature like headings or basic links."
  },
  {
    "section": "Links",
    "example": 516,
    "weight": 1.0,
    "explanation": "Allowing inline formatting (emphasis, strong, code spans) inside link text is a common, foundational pattern in real-world docs and READMEs; incorrect rendering would break link appearance/meaning and be highly confusing."
  },
  {
    "section": "Links",
    "example": 517,
    "weight": 0.9,
    "explanation": "Using an image as the content of a link (clickable images) is a common, practical pattern in READMEs, docs, and web content; failing to render this correctly would create broken or non-clickable images and be visibly confusing, so correct handling is important."
  },
  {
    "section": "Links",
    "example": 518,
    "weight": 0.5,
    "explanation": "This is a specific nesting/precedence rule for links (bracket matching and 'no nested links') — an occasional real-world pattern that can confuse readers if mis-parsed, but it's an edge-case compared with basic link and formatting features."
  },
  {
    "section": "Links",
    "example": 519,
    "weight": 0.5,
    "explanation": "This is an edge case about link-nesting precedence (links cannot contain other links) and interplay with emphasis; nested links are uncommon but brackets and emphasis inside link-like text appear occasionally, so incorrect handling would produce visible and potentially confusing mis-linking but is not foundational."
  },
  {
    "section": "Links",
    "example": 520,
    "weight": 0.3,
    "explanation": "This is a fairly rare, nested-link/image precedence case (how bracketed link syntax is treated inside an image alt text). Incorrect rendering could be confusing (wrong alt text or link targets), but the pattern rarely appears in everyday docs so it's a niche but nontrivial edge case."
  },
  {
    "section": "Links",
    "example": 521,
    "weight": 0.8,
    "explanation": "This tests precedence between link text and emphasis markers — a common combination since links and emphasis are both frequent; getting it wrong can produce misleading emphasis or corrupt links, so incorrect rendering would be noticeably confusing though it's a somewhat specific nesting rule."
  },
  {
    "section": "Links",
    "example": 522,
    "weight": 0.7,
    "explanation": "This tests precedence of link text grouping over emphasis; inline links containing asterisks or emphasis markers are reasonably common and mis-parsing would produce confusing/misleading output (broken emphasis or incorrect link text/destination), so correct handling is important though not as foundational as headings or code blocks."
  },
  {
    "section": "Links",
    "example": 523,
    "weight": 0.5,
    "explanation": "This is an inline parsing-precedence edge case (interaction of emphasis and bracketed text). It appears occasionally in real documents and misrendering can produce confusing emphasis or accidental links, but it's not a fundamental feature like headings or links themselves."
  },
  {
    "section": "Links",
    "example": 524,
    "weight": 0.3,
    "explanation": "This is a niche precedence/edge-case (raw HTML inside bracketed text) that rarely appears in real-world content; getting it wrong can be mildly confusing in specific cases but is not a foundational Markdown feature."
  },
  {
    "section": "Links",
    "example": 525,
    "weight": 0.7,
    "explanation": "This tests the precedence of code spans over link bracket parsing — a common situation in documentation and examples where inline code contains brackets; incorrect handling can produce unintended links or mis-rendered code and be confusing to readers."
  },
  {
    "section": "Links",
    "example": 526,
    "weight": 0.3,
    "explanation": "This is an edge-case about tricky nesting of brackets/angle-bracket autolinks and characters like ']' and ')' inside destinations — something that rarely appears in typical docs. Incorrect rendering could break a particular URL or produce surprising text, but it is not a foundational Markdown feature and is relatively uncommon in real-world content."
  },
  {
    "section": "Links",
    "example": 527,
    "weight": 1.0,
    "explanation": "This tests full reference links (separate link definition with a title), a widely used pattern in READMEs and documentation; incorrect handling would break many real-world links and titles, making it a critical feature."
  },
  {
    "section": "Links",
    "example": 528,
    "weight": 0.8,
    "explanation": "This tests handling of balanced, nested brackets inside link text — a fairly common pattern in technical docs and READMEs (e.g., code or bracketed phrases). Links are a foundational feature, and misparsing here often produces wrong link text or broken links, so correct support is important though slightly more specialized than the simplest link cases."
  },
  {
    "section": "Links",
    "example": 529,
    "weight": 0.7,
    "explanation": "This checks that a backslash-escaped bracket inside link text is treated as a literal character (so the label closes at the real closing bracket and the reference link works). Links are foundational and bracketed text appears often (e.g., code, version strings), so getting this wrong can produce malformed link text or broken links and is therefore important."
  },
  {
    "section": "Links",
    "example": 530,
    "weight": 1.0,
    "explanation": "Very common and foundational: links frequently contain inline formatting (emphasis, strong, code) in real-world docs and READMEs. If a renderer fails to handle inline elements inside link text correctly it would significantly misrepresent content and break basic Markdown expectations."
  },
  {
    "section": "Links",
    "example": 531,
    "weight": 1.0,
    "explanation": "This verifies that an image can serve as the link text using a reference-style link — a very common pattern in READMEs, docs, and web content. Incorrect handling (broken href, missing link, or wrong nesting) would break navigation or visuals and be highly confusing, so this is a critical feature."
  },
  {
    "section": "Links",
    "example": 532,
    "weight": 0.3,
    "explanation": "This tests a relatively rare edge case about nesting and precedence of link parsing (balanced brackets and an inner inline link inside a label for an outer reference link). Such patterns appear infrequently in real docs, so incorrect rendering would be confusing in specific cases but is not a widespread or critical failure."
  },
  {
    "section": "Links",
    "example": 533,
    "weight": 0.4,
    "explanation": "This is a somewhat uncommon, corner-case interaction (nested brackets, reference links and emphasis precedence) that most documents don't use frequently, but when it does occur incorrect rendering is confusing. It's not a foundational feature like basic links/headings, but it affects correctness of link/emphasis parsing enough to be moderately important."
  },
  {
    "section": "Links",
    "example": 534,
    "weight": 0.8,
    "explanation": "This tests the common interaction between links and emphasis (link text binding more tightly than emphasis). Such patterns appear regularly in real documents; getting them wrong can produce incorrect emphasis or broken links and be confusing, so correct handling is important though not quite as fundamental as bare links/headers."
  },
  {
    "section": "Links",
    "example": 535,
    "weight": 0.8,
    "explanation": "This tests the common interaction between links and emphasis (link text grouping taking precedence), which appears fairly often; getting it wrong can break links or produce surprising emphasis, so it’s an important inline-parsing rule even if it’s a somewhat specific precedence case."
  },
  {
    "section": "Links",
    "example": 536,
    "weight": 0.3,
    "explanation": "This is an obscure interaction between link/reference parsing and raw HTML/bracket/quote characters — a rare pattern in real documents. Incorrect rendering would be confusing only in edge cases and does not affect foundational Markdown features."
  },
  {
    "section": "Links",
    "example": 537,
    "weight": 0.7,
    "explanation": "Code spans inside link text are relatively common (people put `code` in link labels) and the precedence rule prevents accidental link parsing; getting this wrong can produce incorrect links or break inline code and confuse readers, so it's an important correctness issue."
  },
  {
    "section": "Links",
    "example": 538,
    "weight": 0.3,
    "explanation": "This is an obscure interaction between link-text bracket matching and angle-bracket autolinks that rarely appears in normal writing (READMEs, docs, chat). Getting it wrong can produce confusing output in specific crafted cases, but it’s not a foundational feature and is unlikely to affect most real-world documents."
  },
  {
    "section": "Links",
    "example": 539,
    "weight": 0.9,
    "explanation": "Reference-link label matching is common in READMEs and docs; case-insensitive (Unicode case-folded) matching is expected and widely relied upon — getting it wrong will frequently break or silently alter links and confuse readers."
  },
  {
    "section": "Links",
    "example": 540,
    "weight": 0.6,
    "explanation": "This tests Unicode case-folding for reference-link label matching (ẞ vs SS). Failures would break reference links in multilingual documents and confuse readers — not as ubiquitous as basic links but important for correct handling of non-ASCII labels."
  },
  {
    "section": "Links",
    "example": 541,
    "weight": 0.5,
    "explanation": "Reference links are common, but multi-line/whitespace-normalized link labels are an occasional edge-case; failure to handle this would break some wrapped or formatted reference definitions and be confusing, but it isn’t as foundational as basic link parsing."
  },
  {
    "section": "Links",
    "example": 542,
    "weight": 0.7,
    "explanation": "This tests a common-but-subtle link parsing rule: whether a space separates bracketed text prevents it from being parsed as a reference link. Links are fundamental and misrendering here can produce unexpected literal bracket text or missing/extra links in real-world docs, so it's important though somewhat niche compared to core link syntax."
  },
  {
    "section": "Links",
    "example": 543,
    "weight": 0.8,
    "explanation": "Reference-style links and the precedence rules for shortcut/full/collapsed references appear frequently in READMEs and docs; getting this wrong can turn intended links into literal bracketed text (or vice versa), which is confusing and alters meaning, so correct handling is important though it's a bit more specific than the most basic link cases."
  },
  {
    "section": "Links",
    "example": 544,
    "weight": 0.7,
    "explanation": "Reference-link resolution is an important part of link handling — duplicates occasionally occur (e.g., merged files or imports), and using the wrong definition could silently misdirect links; it’s not as ubiquitous as basic inline-link syntax but is still important to get right."
  },
  {
    "section": "Links",
    "example": 545,
    "weight": 0.2,
    "explanation": "This tests the obscure behavior of backslash-escapes inside reference link labels (whether an escaped character prevents matching the label). That pattern is rare in real-world docs, so incorrect rendering would be uncommon and only mildly confusing when it occurs."
  },
  {
    "section": "Links",
    "example": 546,
    "weight": 0.3,
    "explanation": "This tests an obscure corner of reference-label parsing (unbalanced/bracket-containing labels). Real-world documents seldom use labels with embedded unbalanced brackets, so failures would be a niche/spec-compliance issue rather than a common, highly confusing break."
  },
  {
    "section": "Links",
    "example": 547,
    "weight": 0.3,
    "explanation": "This is a niche edge case about unescaped brackets inside reference link labels; such patterns are uncommon in real-world docs and incorrect handling would be mildly confusing but not catastrophic compared with failures of core features like headings, inline links, or code blocks."
  },
  {
    "section": "Links",
    "example": 548,
    "weight": 0.3,
    "explanation": "This is a niche edge case about nested/unbalanced brackets in reference link labels; such patterns are uncommon in real-world content and misrendering would rarely confuse readers compared with failures in basic link/heading handling."
  },
  {
    "section": "Links",
    "example": 549,
    "weight": 0.3,
    "explanation": "This exercises backslash-escaping of brackets inside reference link labels — a valid but uncommon pattern. Failure would only affect niche cases where authors intentionally include brackets in labels; it’s not foundational for most Markdown content."
  },
  {
    "section": "Links",
    "example": 550,
    "weight": 0.3,
    "explanation": "This tests a niche escape behavior in reference link labels (a backslash preserved inside the label). While links are common, this particular case (labels ending with an escaped backslash) is rare; incorrect rendering would be confusing in isolated cases but is not widely impactful."
  },
  {
    "section": "Links",
    "example": 551,
    "weight": 0.1,
    "explanation": "Empty link labels are a very rare edge case; most documents don't use `[]` as a label, so misrendering would rarely affect readers — it might turn a literal `[]` into an unintended link (or the reverse), but this is niche rather than foundational."
  },
  {
    "section": "Links",
    "example": 552,
    "weight": 0.3,
    "explanation": "This is a niche edge case about brackets split across lines and invalid reference-definition syntax; it rarely appears in real-world content and mis-rendering would be confusing but not critical compared with core link/heading behavior."
  },
  {
    "section": "Links",
    "example": 553,
    "weight": 0.8,
    "explanation": "Collapsed reference links ([foo][]) are a common, convenient linking style in READMEs and technical docs; failure to support them will produce broken or literal bracketed text and confused readers, so while not as fundamental as inline links they are frequently used and important to render correctly."
  },
  {
    "section": "Links",
    "example": 554,
    "weight": 1.0,
    "explanation": "Reference/collapsed links (including link text containing inline formatting like emphasis) are common in READMEs and docs; failing to parse them correctly would break link targets or show raw markup, which is highly confusing, so this is a fundamental feature."
  },
  {
    "section": "Links",
    "example": 555,
    "weight": 0.8,
    "explanation": "This tests collapsed reference links (case-insensitive label matching), a common variant used in READMEs and docs; failing to support it will silently break links and harm readability, so it's fairly important though slightly less ubiquitous than inline links."
  },
  {
    "section": "Links",
    "example": 556,
    "weight": 0.5,
    "explanation": "This tests a subtle precedence/spacing rule for reference links (collapsed vs shortcut) that can change whether text becomes a link — not as fundamental as headings or inline links but common enough in READMEs and docs that incorrect handling would be confusing."
  },
  {
    "section": "Links",
    "example": 557,
    "weight": 1.0,
    "explanation": "Reference-style links are widely used in READMEs, docs, and other Markdown content; failing to render them breaks basic linking and will confuse readers. This is a foundational Markdown feature, so incorrect or missing support is critical."
  },
  {
    "section": "Links",
    "example": 558,
    "weight": 1.0,
    "explanation": "Allowing inline formatting (like emphasis) inside link text for reference links is a fundamental, commonly used behavior; if a renderer mishandles this the visible link text or emphasis would be lost or rendered incorrectly, breaking clarity and expectations for many real-world documents."
  },
  {
    "section": "Links",
    "example": 559,
    "weight": 0.5,
    "explanation": "This tests handling of balanced brackets and reference-link precedence (a bracketed literal surrounding a reference link). It appears occasionally in READMEs and docs; getting it wrong can break links or show stray brackets, but it's not as ubiquitous or critical as basic inline links or headings."
  },
  {
    "section": "Links",
    "example": 560,
    "weight": 0.3,
    "explanation": "This is a fairly niche parsing-precedence edge case (nested/multiple brackets and reference-link resolution) that seldom appears in typical docs; incorrect rendering would be mildly confusing but not critical compared with core features like basic links, headings, or code blocks."
  },
  {
    "section": "Links",
    "example": 561,
    "weight": 1.0,
    "explanation": "Reference-style links are a fundamental, commonly used Markdown feature in READMEs and documentation; failing to render them correctly would break many links and seriously confuse readers."
  },
  {
    "section": "Links",
    "example": 562,
    "weight": 1.0,
    "explanation": "This tests basic shortcut reference-link behavior, a common and foundational Markdown feature used in READMEs and documentation; failing to support it would break many links and significantly confuse readers."
  },
  {
    "section": "Links",
    "example": 563,
    "weight": 0.7,
    "explanation": "Literal bracketed text (e.g., to show [foo]) is common in docs and comments, and backslash-escaping the `[` to avoid forming a shortcut reference is a practical, frequently-used pattern; if a renderer ignored this it would turn intended literal text into links and confuse readers."
  },
  {
    "section": "Links",
    "example": 564,
    "weight": 0.6,
    "explanation": "This tests the precedence between link text grouping and emphasis (i.e., that brackets bind more tightly than `*`). While the exact pattern (`[foo*]` adjacent to `*`) is not extremely common, links and emphasis frequently appear together, and getting this wrong can break links or produce confusing emphasis, so correct handling is moderately-to-strongly important."
  },
  {
    "section": "Links",
    "example": 565,
    "weight": 0.8,
    "explanation": "Reference-style links like [text][id] are common in READMEs and docs; correct resolution and precedence (full/collapsed vs shortcut) is essential. If a renderer gets this wrong it can produce links pointing to the wrong destination, which is confusing and can break navigation."
  },
  {
    "section": "Links",
    "example": 566,
    "weight": 1.0,
    "explanation": "Reference-style and collapsed links (e.g. [foo][]) are common in READMEs and technical documentation; failing to support or incorrectly render them would break links or show raw markup, causing significant confusion and broken navigation."
  },
  {
    "section": "Links",
    "example": 567,
    "weight": 0.5,
    "explanation": "An inline link with an empty destination (e.g. [foo]()) is an uncommon but real pattern; if a renderer incorrectly falls back to a reference definition instead of treating the destination as empty, links can point to unexpected URLs. This is not foundational but can be moderately confusing and break intended behavior."
  },
  {
    "section": "Links",
    "example": 568,
    "weight": 0.5,
    "explanation": "This tests link-parsing precedence when an apparent inline destination is invalid (contains a space) so the parser should treat [foo] as a reference link and leave “(not a link)” as text. Such ambiguous cases appear occasionally and misrendering can produce wrong or broken links, so it's moderately important but not a foundational feature."
  },
  {
    "section": "Links",
    "example": 569,
    "weight": 0.5,
    "explanation": "This is a moderately useful edge case about reference-link precedence that appears occasionally in documents using many reference-style links; getting it wrong can produce surprising output but it is not a foundational feature used in most Markdown."
  },
  {
    "section": "Links",
    "example": 570,
    "weight": 0.7,
    "explanation": "Reference-style links are fairly common in documentation and READMEs, and incorrect precedence/adjacency parsing (e.g. mishandling [foo][bar][baz]) can produce wrong or missing links, breaking navigation and confusing readers; it's not absolutely fundamental like headings but is an important correctness issue."
  },
  {
    "section": "Links",
    "example": 571,
    "weight": 0.5,
    "explanation": "This tests precedence rules between full/collapsed and shortcut reference links — reference-style links are fairly common and misparsing can produce surprising links or stray literal brackets. It’s not as foundational as basic emphasis or headings, but incorrect behavior would be moderately confusing in real documents."
  },
  {
    "section": "Images",
    "example": 572,
    "weight": 1.0,
    "explanation": "Inline image syntax is a foundational feature used widely in READMEs, docs, blogs and chat output; incorrect handling (including alt text or title extraction) would break visuals and harm accessibility, so failure here is critical."
  },
  {
    "section": "Images",
    "example": 573,
    "weight": 1.0,
    "explanation": "Images and alt text are common in READMEs and docs; getting reference-style images, titles, and especially alt text wrong (e.g., leaving markup in the alt attribute or mis-handling titles/escaping) harms accessibility and misrepresents content, so this is a critical feature."
  },
  {
    "section": "Images",
    "example": 574,
    "weight": 0.5,
    "explanation": "This is an edge case about nested inline elements inside an image description — relatively uncommon in everyday docs but images and correct alt text are foundational for rendering and accessibility; incorrect handling can break parsing or produce wrong alt text, so it’s moderately important."
  },
  {
    "section": "Images",
    "example": 575,
    "weight": 0.5,
    "explanation": "Images and alt text are common and important for accessibility, but embedding link-like markup inside an image description is relatively uncommon; incorrect handling would moderately degrade accessibility and could produce misleading alt text, but it isn't as critical as core block-level features."
  },
  {
    "section": "Images",
    "example": 576,
    "weight": 0.9,
    "explanation": "Images (including reference-style) are common in READMEs and docs and correct alt text is important for accessibility; rendering inline formatting inside the alt or mishandling titles/entities would be misleading and break assistive tools, so this is highly important though slightly more specific than core block elements."
  },
  {
    "section": "Images",
    "example": 577,
    "weight": 1.0,
    "explanation": "Image reference syntax and correct alt/title rendering are fundamental: images are common in docs and READMEs, alt text must be the plain string for accessibility and emphasis must not appear in the alt attribute; incorrect handling would be misleading to readers and assistive tech."
  },
  {
    "section": "Images",
    "example": 578,
    "weight": 1.0,
    "explanation": "Inline image syntax is a fundamental Markdown feature used widely in READMEs, docs, and messages; failing to render images or produce correct alt text breaks content and accessibility and would be highly confusing to readers."
  },
  {
    "section": "Images",
    "example": 579,
    "weight": 1.0,
    "explanation": "Image syntax is a fundamental Markdown feature used frequently in READMEs, docs, blogs and chat; correct rendering (including proper src, title and plain-text alt content for accessibility) is essential and incorrect behavior would be confusing and harmful."
  },
  {
    "section": "Images",
    "example": 580,
    "weight": 1.0,
    "explanation": "Inline image syntax is a foundational Markdown feature used frequently in READMEs, docs, and blogs; failing to render it or to produce correct src/alt attributes would be immediately visible and break content accessibility, so this is critical."
  },
  {
    "section": "Images",
    "example": 581,
    "weight": 1.0,
    "explanation": "Image syntax is a fundamental, frequently used Markdown feature in READMEs, docs, blogs and chat; failing to support or incorrectly render images (including alt/URL handling) would noticeably break common documents and accessibility."
  },
  {
    "section": "Images",
    "example": 582,
    "weight": 1.0,
    "explanation": "Images (including reference-style images) are common in READMEs, docs and chat output and are foundational; incorrect handling (wrong src, wrong alt text, or including formatted/HTML content in alt) breaks accessibility and can be misleading or insecure, so correct support is critical."
  },
  {
    "section": "Images",
    "example": 583,
    "weight": 0.9,
    "explanation": "Reference-style images with case-insensitive labels are common in READMEs and documentation; failing to support them (or treating labels as case-sensitive) will often break or expose raw Markdown instead of images, which is confusing and harms readability."
  },
  {
    "section": "Images",
    "example": 584,
    "weight": 1.0,
    "explanation": "Images (including reference-style images) are common in READMEs, docs, and chat output; failing to parse them or to produce correct src/alt/title (important for display and accessibility) would be a critical break."
  },
  {
    "section": "Images",
    "example": 585,
    "weight": 0.8,
    "explanation": "Image syntax and correct alt-text extraction are common and important for accessibility and correctness; failing to strip inline formatting or mis-handle reference/shortcut labels would produce misleading alt attributes or broken images, so this behavior is fairly important though slightly more specialized than core headings/links."
  },
  {
    "section": "Images",
    "example": 586,
    "weight": 1.0,
    "explanation": "Image syntax (including reference-style and shortcut labels) is a foundational feature used widely in READMEs, docs and chat; incorrect handling breaks images, harms accessibility (alt/title), and would be highly visible and confusing."
  },
  {
    "section": "Images",
    "example": 587,
    "weight": 0.5,
    "explanation": "Images and reference-style links are common, but this particular pattern (an image followed by a lone [] on the next line) is an edge-case of reference parsing; incorrect rendering could be confusing but is not as critical as basic image/link support."
  },
  {
    "section": "Images",
    "example": 588,
    "weight": 1.0,
    "explanation": "Reference-style images are common in READMEs, docs, and posts; failing to support them (or to render alt/title correctly) would frequently break content and accessibility, so correct handling is critical."
  },
  {
    "section": "Images",
    "example": 589,
    "weight": 1.0,
    "explanation": "Image syntax and correct alt-text handling are fundamental—images are common in READMEs and docs and alt text affects accessibility; if a renderer preserves formatting or inline elements instead of the plain string, it will produce incorrect alt attributes and potentially misleading or broken output."
  },
  {
    "section": "Images",
    "example": 590,
    "weight": 0.3,
    "explanation": "This is a niche edge case about nested brackets in image/reference syntax (e.g., '![[foo]]' and wiki-style [[...]]); it rarely appears in typical docs and misrendering would be confusing mainly in specialized cases rather than breaking common content."
  },
  {
    "section": "Images",
    "example": 591,
    "weight": 1.0,
    "explanation": "Reference-style images and correct alt-text extraction are common in READMEs and docs; getting them wrong (failing to resolve references or leaving markup/links inside the alt attribute) breaks image display and accessibility, so this is a foundational feature."
  },
  {
    "section": "Images",
    "example": 592,
    "weight": 0.7,
    "explanation": "Reference-style images and the ability to show literal markdown (via backslash-escaping) appear often in READMEs and docs; if a renderer mishandled this it would confuse examples and produce broken/misleading output. It's not as foundational as headings but is an important, fairly common feature."
  },
  {
    "section": "Images",
    "example": 593,
    "weight": 0.7,
    "explanation": "This checks image-vs-link parsing and escaping a leading '!' — images and links are common in real-world docs, and mishandling can visibly break or misrepresent content. It's not the rarest edge case but is important enough to cause frequent confusion if rendered incorrectly."
  },
  {
    "section": "Autolinks",
    "example": 594,
    "weight": 0.9,
    "explanation": "Autolinks are a common, basic inline-link feature (URLs and emails in <...>) seen frequently in READMEs, docs, and chat; failing to render them correctly would break automatic linking and confuse readers. While not as foundational as headings, they are widely used and important for usability."
  },
  {
    "section": "Autolinks",
    "example": 595,
    "weight": 0.8,
    "explanation": "Autolinks are a common, user-friendly way to include URLs and email addresses and appear often in READMEs, chats, and docs; failing to parse or escape them correctly (e.g., query string ampersands) would frequently break links or confuse readers."
  },
  {
    "section": "Autolinks",
    "example": 596,
    "weight": 0.8,
    "explanation": "Autolinks are commonly used in READMEs, chat, and technical docs to turn raw URIs and email addresses inside angle brackets into clickable links; failing to support them would frequently leave obvious URLs or addresses unlinked and harm usability, though it's slightly less fundamental than basic block-level syntax like headings."
  },
  {
    "section": "Autolinks",
    "example": 597,
    "weight": 0.9,
    "explanation": "Autolinks are a common, fundamental convenience for making raw URIs and email addresses clickable; failing to recognize forms like uppercase schemes or the MAILTO: URI would frequently leave links unlinked or mis-linked and confuse readers."
  },
  {
    "section": "Autolinks",
    "example": 598,
    "weight": 0.7,
    "explanation": "Autolinks are common and some real-world schemes include '+' (e.g. git+ssh, git+https); failing to accept '+' in the scheme would cause valid URIs not to render as links. This is important but not as fundamental as basic heading/paragraph handling."
  },
  {
    "section": "Autolinks",
    "example": 599,
    "weight": 1.0,
    "explanation": "Autolinks (URLs and emails in <...>) are a common, user-friendly way to create links; failure to render them correctly would frequently leave obvious URLs unlinked or display confusing literal angle brackets, harming readability and link functionality."
  },
  {
    "section": "Autolinks",
    "example": 600,
    "weight": 1.0,
    "explanation": "Autolinks are a common, foundational Markdown feature used widely in READMEs, docs, and chat; failing to recognize or render them (even odd-looking URIs like <https://../>) would break many real-world documents and confuse readers."
  },
  {
    "section": "Autolinks",
    "example": 601,
    "weight": 0.8,
    "explanation": "Autolinks are a common, foundational convenience — users often put URIs (including local endpoints like localhost:5001) in angle brackets in READMEs, chats and docs; failing to render them as links would frequently break expected behavior and confuse readers, even if the specific non‑registered scheme example is somewhat less common."
  },
  {
    "section": "Autolinks",
    "example": 602,
    "weight": 0.7,
    "explanation": "Autolinks are a common convenience in real-world Markdown (URLs and emails in <...>), and incorrectly accepting strings with spaces (or producing broken/partial links) would be confusing and produce invalid links; it's important but not as fundamental as headings/paragraphs."
  },
  {
    "section": "Autolinks",
    "example": 603,
    "weight": 1.0,
    "explanation": "Autolinks are a common way to write URLs and emails; mishandling characters inside them (for example failing to percent‑encode reserved characters or incorrectly treating backslashes as escapes) will produce broken or incorrect links and directly harms readability and functionality."
  },
  {
    "section": "Autolinks",
    "example": 604,
    "weight": 0.8,
    "explanation": "Autolinks (e.g., <http://...> and <user@host>) are a common, convenient form of inline links and email addresses in READMEs, chats, and docs; incorrect handling would noticeably break many real-world documents, though explicit link syntax can sometimes compensate."
  },
  {
    "section": "Autolinks",
    "example": 605,
    "weight": 1.0,
    "explanation": "Email autolinks are a common inline-link pattern in docs, READMEs and messages; failing to render them (including generating mailto: links and handling valid characters like + and uppercase) would break expected linking behavior and confuse readers, so this is a critical feature."
  },
  {
    "section": "Autolinks",
    "example": 606,
    "weight": 0.9,
    "explanation": "Autolinks are a common, user-friendly way to include URLs and email addresses (especially in READMEs, chats, and docs); getting them wrong breaks links and confuses readers, so correct support is highly important even if it’s a specific inline-link form."
  },
  {
    "section": "Autolinks",
    "example": 607,
    "weight": 0.3,
    "explanation": "An empty autolink like \"<>\" is a rare edge case; most documents won’t include it, and while misrendering it as a link would be odd, it’s not highly disruptive compared with failures of core features like headings, links, or lists."
  },
  {
    "section": "Autolinks",
    "example": 608,
    "weight": 0.5,
    "explanation": "Autolinks are common, but the specific case of spaces inside angle brackets is an occasional edge case; incorrect rendering could produce unintended links or hide literal text, which is moderately important but not foundational."
  },
  {
    "section": "Autolinks",
    "example": 609,
    "weight": 0.3,
    "explanation": "This tests a niche rule (scheme must be 2–32 chars) for autolinks; single-letter schemes like <m:abc> are uncommon, so failing this is unlikely to affect many real-world docs or cause major confusion compared with core link/URL behavior."
  },
  {
    "section": "Autolinks",
    "example": 610,
    "weight": 0.7,
    "explanation": "Autolinks are a common Markdown feature and angle-bracketed URIs/emails appear frequently; incorrectly turning arbitrary tokens like <foo.bar.baz> into links (or failing to recognize real autolinks) would be confusing and produce broken or misleading links, though it is slightly less foundational than basic block-level features."
  },
  {
    "section": "Autolinks",
    "example": 611,
    "weight": 0.7,
    "explanation": "Bare URLs are very common in real-world text and whether they are autolinked affects readability and behavior; a renderer getting this wrong would be noticeable and could change link semantics, so it's an important (but not absolutely foundational) compatibility issue."
  },
  {
    "section": "Autolinks",
    "example": 612,
    "weight": 0.7,
    "explanation": "Autolinks (especially email addresses and URIs) appear frequently in READMEs, docs and chats, so incorrect handling (e.g., auto-linking bare emails that the spec doesn't, or failing to parse bracketed autolinks) changes clickability and user expectations; it's important but not as foundational as headings or core block structure."
  },
  {
    "section": "Raw HTML",
    "example": 613,
    "weight": 0.8,
    "explanation": "Parsing raw HTML tags is common in real-world Markdown (embedding custom tags, HTML snippets); failing to recognize tags (or mis-handling names with digits) would visibly change output or break embedded content, so correct handling is important though not as universally critical as core elements like headings or links."
  },
  {
    "section": "Raw HTML",
    "example": 614,
    "weight": 0.7,
    "explanation": "Inline/raw HTML is relatively common in real-world Markdown (for <br>, <img>, custom tags, etc.), so failing to recognize simple open/self-closing tags (including tag names with digits) would often change rendering and confuse readers — important but not as foundational as headings or links."
  },
  {
    "section": "Raw HTML",
    "example": 615,
    "weight": 0.5,
    "explanation": "This tests raw-HTML tag parsing (including whitespace/newline in attributes); raw HTML appears fairly often in real-world Markdown and misparsing can break custom markup, but the specific whitespace/newline edge-case is moderately specialized rather than foundational."
  },
  {
    "section": "Raw HTML",
    "example": 616,
    "weight": 0.7,
    "explanation": "Raw HTML in Markdown appears fairly often (custom tags, embedded fragments, attributes) and attributes with quoted values containing angle brackets are a realistic edge that can easily be misparsed; incorrect handling would noticeably change rendered output. It's not as fundamental as headings or links, but correct HTML parsing is important for many real-world documents."
  },
  {
    "section": "Raw HTML",
    "example": 617,
    "weight": 0.8,
    "explanation": "Allowing raw HTML tags (including custom names and self-closing tags) inside paragraphs is common in real-world docs (embedded HTML, custom components) and misrendering would visibly break content or expose raw tags, so correct handling is important though not as universally foundational as headings or links."
  },
  {
    "section": "Raw HTML",
    "example": 618,
    "weight": 0.5,
    "explanation": "Angle-bracket strings that aren’t valid HTML tags (like `<33>` or `<__>`) show up occasionally (emoticons, comparisons, custom tokens); mis-parsing them as raw HTML can silently hide or corrupt content, but it’s not as critical as core features like headings or links."
  },
  {
    "section": "Raw HTML",
    "example": 619,
    "weight": 0.4,
    "explanation": "This is a specific detail of raw-HTML detection (illegal tag/attribute names). It’s not extremely common in everyday Markdown, but getting it wrong can break code examples and HTML-snippet rendering, so it’s moderately important rather than purely niche."
  },
  {
    "section": "Raw HTML",
    "example": 620,
    "weight": 0.6,
    "explanation": "This tests correct recognition of invalid/malformed HTML tags (so they are escaped rather than treated as raw HTML). Raw-HTML handling is common and misparsing can hide text or create rendering/XSS surprises, but this specific malformed-attribute case is relatively uncommon, so it is moderately important."
  },
  {
    "section": "Raw HTML",
    "example": 621,
    "weight": 0.5,
    "explanation": "Recognizing only valid raw-HTML tags (and escaping malformed ones) prevents accidental consumption of text as HTML, which can alter or hide content, but the specific malformed-tag patterns in this example are relatively uncommon in typical documents so it's moderately important rather than critical."
  },
  {
    "section": "Raw HTML",
    "example": 622,
    "weight": 0.4,
    "explanation": "This is a fairly specific edge case (a missing space between attributes makes the tag invalid), so it doesn't occur often in real-world text; however, raw-HTML handling is common and misrendering could confuse readers when authors intend to show literal HTML examples, so the impact is moderate."
  },
  {
    "section": "Raw HTML",
    "example": 623,
    "weight": 0.7,
    "explanation": "Parsing raw HTML tags (including closing tags and slight whitespace variants) is fairly common in real-world Markdown (inline HTML, custom tags, comments); getting it wrong can noticeably break rendering but is not as fundamental as basic blocks like headings or lists, so it's important but not critical."
  },
  {
    "section": "Raw HTML",
    "example": 624,
    "weight": 0.5,
    "explanation": "This tests raw-HTML parsing of an illegal closing tag with attributes — not rare in code/docs showing broken HTML but not foundational. Incorrect handling can hide or mangle example text, so it’s moderately important to get right."
  },
  {
    "section": "Raw HTML",
    "example": 625,
    "weight": 0.8,
    "explanation": "Raw HTML in Markdown is fairly common (for embedding custom tags, comments, tables, etc.), and incorrect handling can visibly break layout or expose HTML/comments to readers; while some specific grammar corner-cases are rarer, overall this is an important feature to get right."
  },
  {
    "section": "Raw HTML",
    "example": 626,
    "weight": 0.3,
    "explanation": "This example exercises obscure raw-HTML comment forms (e.g. <!--> and <!--->). Raw HTML support is useful, but these specific comment-edge cases are rare in real-world content and incorrect rendering would be mildly confusing rather than critical."
  },
  {
    "section": "Raw HTML",
    "example": 627,
    "weight": 0.8,
    "explanation": "Raw HTML in Markdown (including custom tags, comments, and processing instructions like <?php ... ?>) is commonly used in READMEs and docs to control rendering; failure to support it or render it incorrectly will often break layout or show unexpected literal text and confuse readers—though some platforms intentionally sanitize HTML for security."
  },
  {
    "section": "Raw HTML",
    "example": 628,
    "weight": 0.3,
    "explanation": "This example exercises parsing of raw HTML/SGML declarations (e.g. <!ELEMENT ...>), which is an uncommon pattern in typical Markdown documents; while general raw-HTML handling is important, this specific case is relatively niche and misrendering would rarely confuse most readers."
  },
  {
    "section": "Raw HTML",
    "example": 629,
    "weight": 0.3,
    "explanation": "This example exercises raw-HTML parsing for a CDATA section — raw HTML support is useful, but CDATA usage in ordinary Markdown is uncommon. Incorrect rendering would be confusing in XML/embedded-HTML contexts but is a relatively niche edge case for most documents."
  },
  {
    "section": "Raw HTML",
    "example": 630,
    "weight": 0.9,
    "explanation": "Raw HTML support is widely used (inline tags, attributes, custom elements) and getting parsing/escaping of tags and attribute values wrong (e.g., losing entity references) will noticeably break many real-world documents; it's nearly foundational for correct Markdown-to-HTML rendering."
  },
  {
    "section": "Raw HTML",
    "example": 631,
    "weight": 0.3,
    "explanation": "This tests the subtle rule that backslash escapes are not processed inside raw HTML attributes. Embedding raw HTML is common, but backslashes in attribute values are rare; getting this wrong would be a niche bug that is mildly confusing when it occurs but not critical for most documents."
  },
  {
    "section": "Raw HTML",
    "example": 632,
    "weight": 0.7,
    "explanation": "Raw HTML handling is important because many documents include HTML-like tags and incorrect parsing can change meaning or introduce XSS; this particular malformed-attribute / backslash-in-attribute case is somewhat niche but still important to get right, so it’s rated as important rather than critical."
  },
  {
    "section": "Hard line breaks",
    "example": 633,
    "weight": 0.6,
    "explanation": "Double-space or backslash hard line breaks are a common, practical way authors force line breaks (poetry, addresses, short lines); incorrect handling will often harm readability and intent but doesn't break core document structure like headings or links, so it's important but not critical."
  },
  {
    "section": "Hard line breaks",
    "example": 634,
    "weight": 0.7,
    "explanation": "Hard line breaks (two trailing spaces or backslash) are commonly used to force line breaks within paragraphs—important for poetry, addresses, lists, and readability. If rendered incorrectly text can run together and be confusing, so support is important though not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 635,
    "weight": 0.6,
    "explanation": "Trailing-space or backslash hard line breaks are used occasionally (poems, addresses, intentional intra-paragraph breaks) and misrendering will alter layout and meaning in those cases, but this is not as fundamental as headings/links so it is moderately important."
  },
  {
    "section": "Hard line breaks",
    "example": 636,
    "weight": 0.7,
    "explanation": "Hard line breaks are a widely used way to control inline line-wrapping (poems, addresses, lists, manual formatting); getting them wrong will often collapse intended lines and hurt readability, so it's important though not as fundamental as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 637,
    "weight": 0.7,
    "explanation": "Authors commonly use two trailing spaces or a backslash to force line breaks (poetry, addresses, line-oriented prose); if a renderer ignores this, intended line breaks are lost and readability/meaning can be noticeably affected, so support is important though not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 638,
    "weight": 0.7,
    "explanation": "Hard line breaks are a commonly used way to force line breaks in inline text (and are often relied on in prose, READMEs and chat output); incorrect rendering would frequently confuse readers though it’s not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 639,
    "weight": 0.7,
    "explanation": "Hard line breaks are a fairly common way authors force intra-paragraph breaks (poetry, addresses, lists, inline formatting) and incorrect handling can noticeably change meaning or layout, though it's not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 640,
    "weight": 0.7,
    "explanation": "Two-space/backslash hard line breaks are a common way authors control inline line breaks (poems, addresses, deliberate wrapping) and appear regularly in READMEs and chat; missing or incorrect rendering won't break document structure but will noticeably change intended layout and can confuse readers, so it is important though not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 641,
    "weight": 0.6,
    "explanation": "Hard line breaks (two-space or backslash) are fairly common in practice for poetry, addresses, and controlled inline layout; incorrect handling won't break document structure like missing headings or links but will noticeably harm readability and intent, so it's moderately important."
  },
  {
    "section": "Hard line breaks",
    "example": 642,
    "weight": 0.7,
    "explanation": "Trailing two-space (or backslash) hard line breaks are fairly common for intentional line breaks (addresses, poetry, stanzas, lists) and removing them can noticeably harm readability; not as critical as headings/links but still important to render correctly."
  },
  {
    "section": "Hard line breaks",
    "example": 643,
    "weight": 0.5,
    "explanation": "Hard line breaks are moderately used—authors sometimes rely on two trailing spaces or a backslash to force newlines (poetry, addresses, manual line formatting), so lack of support or incorrect rendering would occasionally confuse layout but is not as critical as core features like headings, links, or code blocks."
  },
  {
    "section": "Hard line breaks",
    "example": 644,
    "weight": 0.7,
    "explanation": "Hard line breaks (two trailing spaces or a backslash) are a fairly common idiom for controlling line layout (poetry, addresses, forced breaks in prose) and many users expect them; incorrect rendering would change visible structure and can be confusing, though it isn't as fundamental as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 645,
    "weight": 0.7,
    "explanation": "Two-space or backslash hard line breaks are a fairly common Markdown idiom for forcing inline line breaks (poems, addresses, manual formatting) and appear often enough in READMEs and prose that incorrect handling would noticeably alter formatting, though it's not as foundational as headings or links."
  },
  {
    "section": "Hard line breaks",
    "example": 646,
    "weight": 0.5,
    "explanation": "Hard line breaks (two trailing spaces or a trailing backslash) are moderately common and affect visible inline layout (useful for poetry, headers, and README formatting); incorrect rendering changes the appearance but rarely breaks document semantics, so it's important but not foundational."
  },
  {
    "section": "Hard line breaks",
    "example": 647,
    "weight": 0.5,
    "explanation": "Hard line breaks (two trailing spaces or backslash) are a moderately used formatting convenience (poetry, addresses, manual line breaks) and getting them wrong would cause noticeable but not critical rendering errors; the end-of-block exception is a subtle spec detail that rarely breaks core readability."
  },
  {
    "section": "Soft line breaks",
    "example": 648,
    "weight": 1.0,
    "explanation": "Single-line (soft) breaks appear very frequently in real-world Markdown (wrapped lines, AI output, READMEs) and affect basic paragraph flow; incorrect handling (treating them as hard breaks or dropping spacing) would noticeably change readability and meaning, so support is foundational."
  },
  {
    "section": "Soft line breaks",
    "example": 649,
    "weight": 0.9,
    "explanation": "Soft line breaks are a fundamental part of paragraph formatting and appear frequently in real-world Markdown; incorrect handling would regularly alter text flow and confuse readers, so correct behavior is highly important."
  },
  {
    "section": "Textual content",
    "example": 650,
    "weight": 1.0,
    "explanation": "Rendering plain textual content and preserving internal spaces/punctuation is foundational — virtually every document relies on it. Incorrect behavior would break basic readability and confuse users across almost all Markdown use cases."
  },
  {
    "section": "Textual content",
    "example": 651,
    "weight": 1.0,
    "explanation": "This verifies that arbitrary Unicode characters are treated as plain text and preserved in paragraphs — a foundational behavior affecting many real-world documents; incorrect Unicode handling would be common and highly confusing."
  },
  {
    "section": "Textual content",
    "example": 652,
    "weight": 0.3,
    "explanation": "Preserving internal spaces in the source is an implementation detail that rarely affects typical documents (authors usually use code/pre for significant spacing), so incorrect rendering would be uncommon and not highly confusing for most readers."
  },
  {
    "section": "Tabs",
    "example": 1,
    "weight": 0.6,
    "explanation": "Tabs-as-4-space behavior affects detection of indented code blocks and blockquote content and while many users prefer fenced code blocks, tabs are still commonly used and mis-handling can easily turn intended code into normal text, causing confusing output. This is important but not as foundational as headings/links, so a moderately-high weight is appropriate."
  },
  {
    "section": "Tabs",
    "example": 2,
    "weight": 0.4,
    "explanation": "Tabs-as-4-space indentation affects how indented code blocks and nested block structures (e.g., list or blockquote continuations) are recognized; authors sometimes use tabs so incorrect handling can break code/structure, but fenced code is common so this is a moderately important edge case rather than foundational."
  },
  {
    "section": "Tabs",
    "example": 3,
    "weight": 0.5,
    "explanation": "Tabs used as indentation for code blocks and inside block quotes occur occasionally (though fenced code blocks and spaces are more common); incorrect handling (wrong tab-stop expansion or ignoring tabs) can misclassify content as code or regular text and be confusing, but it is not as foundational as headings or links."
  },
  {
    "section": "Tabs",
    "example": 4,
    "weight": 0.5,
    "explanation": "Tabs used as indentation appear occasionally (e.g., pasted text, tab-using authors) and mis-handling can accidentally create code blocks or break list/blockquote structure, which is moderately impactful but not fundamental."
  },
  {
    "section": "Tabs",
    "example": 5,
    "weight": 0.5,
    "explanation": "Tabs in indentation are fairly common (people often use tabs to indent code or continuation lines in lists/blockquotes), and getting tab-stop behavior wrong can change paragraphs into code blocks or break list structure, but the specific tab-stop/delimiter interaction is a relatively narrow, non-foundational rule compared with core features like headings or links."
  },
  {
    "section": "Tabs",
    "example": 6,
    "weight": 0.3,
    "explanation": "This is a niche interaction of tab stop semantics with blockquote delimiters and code-block indentation; most authors use spaces rather than tabs, so it rarely appears. Incorrect rendering would be confusing in specific documents that use tabs for indentation, but it is not foundational for typical Markdown usage."
  },
  {
    "section": "Tabs",
    "example": 7,
    "weight": 0.7,
    "explanation": "Many authors use tabs for indentation (especially in code/list contexts); incorrect tab-stop handling can break list continuations and indented code blocks and produce confusing output. It’s not as fundamental as headings/links but is a common, user-facing behavior that renderers should get right."
  },
  {
    "section": "Tabs",
    "example": 8,
    "weight": 0.5,
    "explanation": "Tabs-as-4-space behavior affects whether indented code blocks and continued list/blockquote lines are recognized. Fenced code is common, but many real-world files and email-style Markdown still use indented code or tabs, so incorrect handling can sporadically break important blocks and confuse readers."
  },
  {
    "section": "Tabs",
    "example": 9,
    "weight": 0.7,
    "explanation": "Tabs are commonly used for indentation and affect list nesting and code block detection; if a renderer mishandles tab stops (so list items become code blocks or wrong nesting) it will frequently break document structure and confuse readers."
  },
  {
    "section": "Tabs",
    "example": 10,
    "weight": 0.7,
    "explanation": "Tab handling affects core block structure (headings, lists, blockquotes and indented code); mis-handling tabs can break common constructs and confuse readers even though authors don't always use tabs intentionally."
  },
  {
    "section": "Tabs",
    "example": 11,
    "weight": 0.3,
    "explanation": "This example tests a narrow tab-handling detail (tabs between asterisks producing a thematic break). It's an uncommon pattern in real-world content, so incorrect rendering would be mildly confusing but not critical compared with core Markdown features."
  }
]